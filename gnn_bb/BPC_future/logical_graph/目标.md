你现在进入目标模式。请沿着当前 Sharded Pulse Final Judge 路线继续优化，目标是在保证 exactness 的前提下，取得真实求解性能提升。

这次目标不是“20-task smoke 有日志”，而是：

总目标：
1. 5-task 全量必须无回退：
   - OPTIMAL count 不下降；
   - objective / dual_bound 不错；
   - 平均时间、最大时间不能明显劣化；
   - 不允许 Apollo5 / Tranquillitatis5 这类小快实例被 Pulse audit/worker 拖成 TIME_LIMIT。
2. 10-task 必须无回退：
   - OPTIMAL count 不下降；
   - hard cases 不恶化；
   - 目标减少 TIME_LIMIT、降低 tail exact pricing / completion-bound retry 时间；
   - 不能为了 20-task 改善牺牲 10-task。
3. 20-task 必须明显改善和加速求解：
   - 不只是 smoke；
   - 至少在 selected 20-task hard smoke 上 wall time / gap / primal / generated / evaluated / final judge time / hidden negative / retry 指标有明确改善；
   - 最终目标是更多有效列、更少 final judge tail、更低 gap 或更多 OPTIMAL；
   - 不允许仅靠 timeout 早停制造“加速”假象。
4. 任何性能提升都必须保持 exactness：
   - official lower bound 只能来自 true-dual exact pricing certificate；
   - Pulse worker 找列可以加入 RMP；
   - Pulse incomplete / duplicate-only / empty-harvest / no-column 不能更新 official bound；
   - smoothed/GNN dual 不能参与 certificate；
   - default config 不得被污染。

当前已完成背景：
- Phase 1-2.5：sharded ledger、dummy engine、driver smoke、dummy production guard。
- Phase 3A：Pulse leaf materialization contract；completed sortie 必须回放 evaluate_timed_trip()；journey 必须用 make_journey()；true RC 必须用 manual_journey_reduced_cost()。
- Phase 3B-7A：toy exhaustive / transition-level root-only Pulse 与 brute-force 对齐。
- Phase 7B：guarded sharded path 已改用 transition_root_only_pulse()。
- Phase 7C：StructuralKeyDominanceArchive 接入 transition state。
- Phase 7D：transition-state harvesting 接入。
- Phase 7E：PrefixReducedCostLedger 与 weak safe bound pruning。
- Phase 7F：bound ROI diagnostics。
- Phase 7G：no-wait start interval / candidate start-time completeness。
- Phase 7H：audit-only legacy-equivalence smoke。
- Phase 7I-A / 7M / 7N：hidden-negative worker 与 current-context signal probe。
- Phase 7J：adaptive second-action shard refinement。
- Phase 7K：shard scheduling、ROI gate、audit trigger observability、hard-tail worker gate。
- Phase 7L：audit-only ROI calibration。
- Phase 7M/7N 后的结论：Pulse current-context probe 已能在 Apollo10 / tranq10_09 返回并加入 true-RC negative columns，但还没有证明整体 wall-time ROI。
- Phase 7U：加入 RMP / column-pool structure diagnostics；20-task smoke 显示 worker 新列能进入 pool 并改变 active support，但 residual ordinary/exact pricing negative tail 仍存在，当前问题不是单纯 task-set duplicate pool pressure。
- Phase 7V：加入 residual pricing / legacy tail attribution；`journey_pricing` 与 `journey_column_addition` 只读记录 capped task-set samples，ROI summary 可计算 worker 后首个 follow-up negative 与 worker 变更 task-set 的 overlap / Jaccard；不改变求解路径、worker gate、certificate 或 official lower bound。
- Phase 7W：修正 ROI summary 的 same-iteration follow-up 归因，并跑 20-task residual-tail attribution；Apollo 加深 probe 显示 worker 加入 `[6,19]` 后，ordinary heuristic 后续返回 `[5,8,15]`、`[5,12,18]`、`[12,16,17]` 等 disjoint new task-set negatives，说明当前 worker 没覆盖 ordinary negative tail，不支持继续加 worker budget 或打开 certificate gate。
- Phase 7X：比较 natural 与 `reduced_cost_proxy` worker ordering；两者 worker 首列仍都是 `[6,19]`，ordinary follow-up 首个 residual negative 仍是 `[5,8,15]` 且 relation 为 `disjoint_task_set`，说明现有 ordering 没修复 worker coverage 缺口，active-worker gate stacking 方向应暂停。
- Phase 7Y：新增 20-task-only coverage-scan diagnostic profile，在相同小预算下关闭 `stop_after_first_negative`；Apollo probe 显示 worker 递归从 115 增至 245、time-window prune 从 5930 增至 12666，但仍只返回 `[6,19]`，ordinary follow-up 首个 residual negative 仍是 `[5,8,15]`。这说明 coverage 缺口不是单纯 early-stop / shard-ordering 问题，继续堆 active worker gate 或扩大 worker 时间没有依据。
- Phase 7Z：补 worker shard summary 字段，并新增 no-ROI-gate coverage profile；Apollo probe 显示关闭 shard ROI gate 后仍只返回 `[6,19]`，未覆盖 ordinary follow-up 的 `[5,8,15]`。coverage-scan 可偶发多返回 `[7,19]`，但 residual family 仍缺失，且 20 个 first-task shards 中多数仍 incomplete；当前主因不是 ROI gate 单点，而是小预算下 Pulse 候选子空间覆盖不足或与 ordinary heuristic/profile-DP 的候选语义存在差异。
- Phase 7AA：补 negative journey sequence/signature samples；Apollo probe 显示 worker negative 是 sequence `[6,19]`、signature start `241.411702`，ordinary follow-up 首个 residual negative 是 sequence `[8,15,5]`、start `0.0`，第二个 residual 是 `[12,18,5]`、start `3.086313`。因此 coverage 缺口可定位到具体 first-task shard/sequence family，下一步应做同 context targeted replay，而不是继续调 worker gate。
- Phase 7AB：新增 opt-in residual replay diagnostic；对 ordinary follow-up `[8,15,5]` 和 `[12,18,5]` 直接用 Phase 3A Pulse leaf materialization 回放，结果均 `materialized`，signature mismatch = 0，RC delta = 0.0，true RC 分别保持 `-138.437225` / `-128.547499`。这排除了 leaf materialization / true-dual context mismatch，当前 coverage 缺口集中在 transition DFS / shard scheduling / search-budget 没进入这些 early-start sequence family。
- Phase 7AC：新增 opt-in target-sequence reachability diagnostic；coverage profiles 对 worker 目标 sequence `[8,15,5]` 记录 prefix reachability / transition attempts / prune reason。Apollo20 rerun 显示在目标 first-task shard 作用域内 `reached_prefix_len=0`、`transition_attempts=0`、`blocked_reason=deadline`，说明小预算 worker 不是通过 time/resource/bound/archive 剪掉 `[8,15,5]`，而是在 first-task shard 8 开始扩展目标 transition 前已耗尽 deadline。当前缺口进一步定位为 shard scheduling / per-shard budget allocation 问题，而不是 leaf materialization 或 transition feasibility rule 误剪。
- Phase 7AD：新增 opt-in target first-task shard priority diagnostic；仅在 20-task coverage target-priority profile 中把目标 sequence `[8,15,5]` 的 first-task shard `8` 提前，其余 shards 不丢弃、不证书化。Apollo20 窄 probe 显示旧 coverage-scan 仍 `reached_prefix_len=0` / `transition_attempts=0` / `blocked_reason=deadline`；target-priority profile 变为 `reached_prefix_len=1` / `transition_attempts=1` / `transition_accepted=1`，worker 返回两个以 `8` 开头的新列 `[8,4,18]`、`[8,4]`，但目标 `[8,15,5]` 仍未 completed/materialized，blocked reason 仍为 deadline。当前结论：first-task shard priority 能修复“完全没进入 shard 8”的问题，但目标 family 缺口已进一步收窄到 shard 8 内部 transition/task ordering 或 per-shard budget。
- Phase 7AE：新增 opt-in target transition priority diagnostic；当当前 prefix 匹配目标 sequence 前缀时，把下一目标 task 移到该层 transition 候选最前，其余候选不丢弃、不证书化。Apollo20 窄 probe 显示 target-transition profile 进一步从 7AD 的 `reached_prefix_len=1` 推进到 `reached_prefix_len=2`，`transition_attempts=9`、`transition_accepted=3`，worker 返回 `[8,15,18,4]`、`[8,15,18]`、`[8,15,4]`、`[8,15]` 等以 `[8,15]` 开头的新列。但目标 `[8,15,5]` 仍未 materialized，当前 blocked reason 变为 `time_window`、blocked prefix `[8,15]`、next task `5`，prune reason counts 为 `time_window=6`。结合 7AB 已证明 `[8,15,5]` 的 residual replay 可物化且 RC 一致，当前缺口进一步定位到目标 prefix 下的 arc-option / start-time path ordering 或 path-specific transition state，而不是 first-task shard 或 next-task ordering。
- Phase 7AF：新增 opt-in target path diagnostics；记录目标前缀 state 的 `arc_ids/start_lb/start_ub/offset/current_time`，以及目标 transition prune 的 option id 和 time-window 计算。Apollo20 窄 probe 显示 target path profile 与 7AE 一样达到 `[8,15]` 后在 `15 -> 5` time-window prune，但 path samples 揭示 worker prefix 使用 `0->8:low_risk:2`，而 7AB residual replay 的可行 signature 使用 `0->8:low_time:0`；worker 已尝试 `15->5:low_risk:2/low_time:0/low_energy:1`，但在当前 prefix offset 下 `next_start_ub` 均为负。当前缺口进一步定位为 target prefix 的 arc-option ordering：应优先探索 residual replay 对应的 `0->8:low_time:0`, `8->15:low_risk:2`, `15->5:low_risk:2`, `5->0:low_time:0` option family，而不是继续调 first-task 或 next-task ordering。
- Phase 7AG：新增 opt-in target arc-option priority diagnostic；当当前 target prefix 需要扩展下一条边时，只把指定 arc option id 提到该边候选最前，其余 option 不丢弃、不证书化。Apollo20 窄 probe 显示该开关确实把 target prefix 的首条边从 `0->8:low_risk:2` 改为 residual replay 中的 `0->8:low_time:0`，prefix sample 变为 `prefix=8;arc_ids=0->8:low_time:0`；但在相同低预算和 1.0s 诊断预算下，target sequence 只达到 prefix length 1，blocked reason 为 `deadline`，worker 返回/加入列均为 0。当前结论：7AG 证明 arc-option priority 接线和诊断有效，但没有改善 `[8,15,5]` coverage，也不是 ROI 正信号；下一步应做目标 path/start-time 的局部 replay/transition ordering 对照或停止继续叠 active worker gate，而不是放开 certificate 或扩大默认 worker。
- Phase 7AH：新增 active-worker closure audit；汇总 7O-7AG 的 worker A/B、coverage scan、residual attribution、target diagnostics 与 7J refinement 证据。结论是 current-context Pulse hidden-negative active-worker 子路线安全但无稳定 ROI：worker 能加 true-RC negative columns，但 5/10 no-regression 主要靠 gate 关闭 worker，20-task 没有稳定 wall-time / proof-tail 改善，worker 后 residual negative tail 仍存在，target-specific ordering 也未覆盖 `[8,15,5]`。因此停止继续扩大 active worker gate/budget/target ordering，不开启 official certificate gate，不默认启用 Pulse worker。注意：这不是整个目标完成声明，因为 proof-closed resume 未实现/未证明，最终条件 B 的 `refinement/resume 无降低 incomplete` 尚未完全满足；下一步应转向 RMP stabilization / pool compression / legacy proof-tail optimization，或单独实现 proof-closed resume 补齐 B 条件证据。
- Phase 8A：新增 ROI summary pivot classifier；`run_sharded_pulse_roi_calibration.py` 现在输出 `pivot_recommendation_class/reason`，只读分类 correctness blocker、profile-DP state cap、residual disjoint negative、pool duplicate pressure、RMP fractional active pressure 等。very_small smoke 证明字段落盘；离线套到 7U/7W/7Z/7AG summary 后，主要信号是 `residual_disjoint_negative`、`profile_dp_state_cap` 与 20-task `rmp_fractional_active_pressure`，而不是 pool duplicate pressure。当前 pivot 结论：停止 Pulse active-worker gate 扩张后，下一步更应进入 legacy/profile-DP proof-tail 或 RMP stabilization / active fractional degeneracy，而不是继续做 target-specific Pulse ordering。
- Phase 8B：做 profile-DP state-cap sensitivity 窄 probe；在 `mt20_greedy_apollo_01` 上比较 `journey_pricing_max_dp_states=1000/5000`，短 cap 0.3s 下提高 cap 可让 pricing path 从 `INCOMPLETE_LIMIT` 变为 `FOUND_NEGATIVE`，但 primal/wall 不形成稳定收益；1.5s 下没有复现 `profile_dp_state_cap` incomplete，worker follow-up 仍是 `residual_disjoint_negative`，提高 cap 只增加搜索量与 wall。结论：不要简单提高 `journey_pricing_max_dp_states`；若继续 proof-tail，应先做 profile-DP state explosion 的结构化归因，或转向 RMP stabilization / active fractional degeneracy。
- Phase 8C：新增 profile-DP state explosion 只读归因；`record_dp_stats()` 现在输出 `nonempty_mask_count`、`max_labels_per_mask_observed`、`labels_by_sortie_count`、`top_mask_label_counts`，并透传到 `journey_pricing` JSONL、audit stats 与 ROI summary 的 `followup_profile_dp_*` / `pulse_worker_followup_profile_dp_*` 字段。该 phase 不改变 DP 转移、剪枝、worker trigger、certificate 或 official lower bound，只让后续 profile-DP cap / tail 诊断能区分“少数 mask bucket 爆炸”和“整体 reachable mask 面扩张”。very_small summary smoke 已证明字段落盘。
- Phase 8D：用 8C 字段跑 profile-DP state attribution 窄 probe；Apollo20 cap1000 显示约 `130` 个 nonempty masks、最大 bucket `24`，cap5000 进入 2-sortie 层后 nonempty masks 增至 `343-439`、最大 bucket `63`，worker 后 residual first negative 仍是 disjoint `5,8,15`；Tranq20 cap1000 为约 `90` 个 nonempty masks、最大 bucket `30`，worker 未加列。结论：当前 profile-DP tail 更像 broad reachable-mask expansion + 二层 sortie 组合扩张，而不是单一 bucket 灾难；不要继续简单提高 cap，也不能据此打开 Pulse worker default 或 certificate gate。
- Phase 8E：新增 RMP active fractional / pool pressure 只读归因；`journey_pool_structure_diagnostics` 现在输出 active duplicate ratio、active avg journeys/task-set、fractional value min/max/sum、小权重 fractional count、top active task-set value samples，并透传到 ROI summary。Apollo/Tranq 20-task 窄 probe 显示 active duplicate ratio 均为 `0.0`、active avg journeys/task-set 均为 `1.0`；出现 fractional 的 `tranq20_01` 也只是 3 个 `0.5` 权重列，不是大量 tiny fractional 或 duplicate active task-set。结论：当前 hard-tail 不支持把 column-pool duplicate compression 作为主线，除非后续更大矩阵出现高 active duplicate / tiny fractional 信号；更应继续 residual pricing / legacy proof-tail 或 profile-DP structural control。
- Phase 8F：新增 residual negative 与 profile-DP top-mask 的只读归因；ROI summary 现在比较 worker 后首个 follow-up negative task-set 和 `dp_top_mask_label_counts` 的 overlap/jaccard/exact。Apollo20 cap1000/cap5000 均显示 worker 后 residual `5,8,15` 不是 profile-DP top-mask exact hit：cap1000 只是 overlap=2、jaccard=0.5，cap5000 overlap=2、jaccard=0.333333333，且 top masks 转向 2-sortie 大 task-set。结论：直接针对 top overloaded masks 做 materialization 不一定覆盖 residual tail；下一步若继续 profile-DP structural control，应关注 residual task-set / active task-set 定向 materialization、rough-vs-true RC 排序偏差，或 ordinary heuristic 为什么稳定找到 `[5,8,15]`。
- 当前关键约束：之前 Phase 3B-6 抽样显示 completed-trace toy path 没有性能收益，在 5/10 上还会导致证书丢失，不能把 toy path 当 production 求解器。必须基于 transition-level Pulse、strict gate、support-aware harvesting、ROI 数据继续推进。:contentReference[oaicite:1]{index=1}

请先阅读并理解这些文件：
- BPC_future/pricing/pulse_toy_exhaustive.py
- BPC_future/pricing/pulse_materialization.py
- BPC_future/pricing/pulse_archive.py
- BPC_future/pricing/sharded_pulse_final_judge.py
- BPC_future/pricing/journey_pricing.py
- BPC_future/pricing/journey_harvesting.py
- BPC_future/solver/journey_driver.py
- BPC_future/core/journey.py
- BPC_future/core/columns.py
- BPC_future/core/cuts.py
- BPC_future/core/branching.py
- BPC_future/master/journey_rmp.py
- BPC_future/docs/sharded_pulse_final_judge_plan.md
- 最新 run_reports 中所有 sharded_pulse phase 报告
- BPC_future/tests/test_bpc_future.py
- BPC_future/scripts/run_sharded_pulse_roi_calibration.py，如果存在
- BPC_future/results 中最新 sharded pulse worker / audit / ROI 结果目录

最高优先级原则：
- 永远不要牺牲 exactness 换速度。
- 默认 benchmark 行为必须保持不变，除非新 profile 显式开启。
- 5-task 和 10-task 是 regression gate；20-task 是 improvement target。
- INCOMPLETE / TIME_LIMIT / RECURSION_LIMIT / LOW_ROI_INCOMPLETE / DUPLICATE_ONLY / EMPTY_HARVEST 不能更新 official lower bound。
- 所有返回给 RMP 的 Pulse journeys 必须逐条 manual_journey_reduced_cost() < -eps。
- forbidden signatures、cuts、branch constraints、true dual context 必须一致；context mismatch 必须 fail-closed。
- 如果优化让 5-task 或 10-task 明显变差，必须自动关闭该 profile或加更严格 gate。
- 不允许把“少算了 final judge 所以更快”当作有效提升；必须确保 official result/gap semantics 不变。

============================================================
Phase 7O：Hard-tail worker ROI A/B，带 5/10 no-regression gate 和 20 improvement gate
============================================================

目标：
验证 current-context Pulse worker 加入的 true-RC negative columns 是否真的减少 legacy final judge tail，而不是只增加额外开销。

请不要先写新算法。先增强/复用 run_sharded_pulse_roi_calibration.py 或新增脚本，跑 hard-tail worker ROI A/B。

实例矩阵必须分三层：

A. 5-task regression gate：
- Apollo5
- Tranquillitatis5
- 若有全量 5-task set，跑全量；没有则至少跑上述两个代表。
目标：
- OPTIMAL count 不下降；
- wall time 不明显增加；
- 不能从 OPTIMAL 变 TIME_LIMIT；
- audit/worker 不能拖慢小快实例。

B. 10-task regression + hard-tail gate：
至少包括：
- Apollo10
- tranq10_09
- tranq10_04
- tranq10_01
- tranq10_06
- apollo10_04
- apollo10_09
目标：
- OPTIMAL count 不下降；
- TIME_LIMIT 不增加；
- hard-tail retry / hidden negative / final judge time 有下降信号；
- 若某 profile改善20但恶化10，不能作为候选。

C. 20-task improvement gate：
至少包括：
- tranq20_01
- 再选 2-5 个 20-task hard smoke，若数据/config 可用；
- Apollo20 需要先做 single-task feasibility preflight；若实例 grid 不可行，不计为算法失败。
目标：
- wall time 下降，或同时间下 gap 下降；
- primal 更好或列质量更好；
- generated/evaluated/final judge tail 指标下降；
- 不能只是提前 INCOMPLETE 造成“快”。

profiles 至少包括：
1. baseline
2. audit_only
3. strict_worker_previous_signal_only
4. strict_worker_current_probe
5. strict_worker_current_probe_support_aware
6. strict_worker_current_probe_support_aware_low_budget
7. strict_worker_current_probe_support_aware_mid_budget
8. strict_worker_current_probe_support_aware_impact_filter
9. strict_worker_current_probe_hard_tail_only

所有 Pulse worker profile 仍默认 opt-in；不能改默认 benchmark。

A/B 输出目录：
BPC_future/results/sharded_pulse_phase7o_worker_roi_ab_<date>/

输出文件：
- summary.json
- summary.csv
- per-run JSONL logs
- report: BPC_future/logical_graph/run_reports/20260612_sharded_pulse_phase7o_worker_roi_ab_zh.md

summary 每行必须包含：
- instance
- scale: 5 / 10 / 20
- profile
- status
- wall_time
- primal
- dual_bound
- gap
- pricing_state
- best_rc
- official_result_changed_vs_baseline
- objective_mismatch_vs_baseline
- critical_disagreement_count
- legacy_final_judge_calls
- exact_completion_bound_retry_count
- exact_completion_bound_retry_time
- hidden_negative_audit_count
- pricing_calls
- root_rmp_rounds
- columns
- generated_sequences
- evaluated_timed_trips
- final_judge_max_single_call_time
- worker_triggered
- worker_skip_reason
- worker_signal_source
- worker_context_hash
- worker_returned_journeys
- worker_added_journeys
- worker_added_new_task_set_count
- worker_added_replacement_count
- worker_added_support_changing_count
- worker_addition_productivity_class
- worker_time
- worker_recursions
- worker_pruned_total
- worker_bound_pruned
- worker_archive_pruned
- worker_time_window_pruned
- worker_return_pruned
- worker_harvested_count
- followup_rmp_objective_delta
- followup_dual_l1_delta
- followup_legacy_final_judge_called
- followup_completion_retry_called
- improvement_class:
  - no_regression
  - improved
  - worsened
  - unsafe
  - inconclusive

如果某些字段不能直接获得，请从 JSONL 日志重建；如果仍不可得，添加日志字段，但不能改变 solver 语义。

Phase 7O 成功标准必须同时满足：
- 5-task no-regression：
  - OPTIMAL count 不下降；
  - average wall time 增加不超过 5% 或 0.2s，取较大者；
  - max wall time 不明显增加；
  - no false certificate；
  - no critical disagreement。
- 10-task no-regression：
  - OPTIMAL count 不下降；
  - TIME_LIMIT count 不增加；
  - median wall time 不增加超过 5%；
  - hard cases 至少一个 tail metric 改善。
- 20-task improvement：
  - 至少一个 20-task hard smoke 有明显改善：
    - wall time 下降 >= 15%，或
    - same time limit 下 gap 下降 >= 10%，或
    - final judge / completion-bound retry time 下降 >= 25%，或
    - worker 加列后 primal 改善 / columns质量改善 / hidden negative retry下降；
  - 不能牺牲 5/10 换20。
- exactness：
  - no critical disagreement；
  - all worker-returned journeys true-RC negative；
  - no official lower bound from incomplete Pulse.

如果 Phase 7O 发现 worker 没 ROI：
- 不要继续扩大 worker time limit；
- 不要默认启用 worker；
- 转入 Phase 7P-alt：column impact filter / active-support-aware return。
如果 Phase 7O 发现 worker 有 20 改善但 5/10 回退：
- 增加更严格 scale/hard-tail gate；
- 该 profile不能作为候选。
如果 Phase 7O 发现 worker 有 ROI 且 5/10 无回退：
- 进入 Phase 7P：hard-tail worker production tuning。
如果出现 critical disagreement：
- 停止性能优化；
- 输出 correctness diagnosis；
- 检查 true dual / cuts / branch / forbidden hash / manual_journey_reduced_cost / candidate_start_times / materialization universe；
- 不得继续推进 worker 或 certificate。

============================================================
Phase 7P：Hard-tail worker production tuning，必须保持 5/10 no-regression
============================================================

只有 Phase 7O 同时满足 5/10 no-regression 和 20 improvement 才执行。

目标：
把 Pulse hidden-negative worker 调成严格 hard-tail-only，而不是默认 worker。

实现要求：
1. worker 仅在 hard-tail fingerprints 下触发：
   - certificate_candidate=True；
   - remaining_time >= min_remaining_time；
   - num_tasks >= min_tasks；
   - scale >= configured_min_scale；
   - previous audit negative 或 current-context probe negative；
   - 或同一 node 最近 N 轮出现 legacy_incomplete_pulse_negative；
   - 或 hidden_negative_audit_count / completion-bound retry count 超过阈值。
2. 增加 scale-specific gate：
   - 5-task 默认禁止 active worker，除非显式 force；
   - 10-task 只在 hard-tail fingerprint 下启用；
   - 20-task 可更积极，但仍必须 bounded。
3. 增加 success memory：
   - 如果某 node / instance / context 类似情况下 worker 成功加列，则未来 N 个 CG iterations 可降低触发门槛；
   - 如果 worker 连续 K 次无列 / duplicate-only / incomplete，则 cooldown M 轮。
4. 增加 worker budget 自适应：
   - 5-task：0 或极小；
   - 10-task：小预算；
   - 20-task：中等预算，但绝不超过 remaining-time reserve；
   - 若 worker近期成功且 remaining_time 充足，允许轻微增加；
   - 若 low ROI / small-fast instance，强制关闭。
5. support-aware harvesting 参数调优：
   - 优先 new task-set；
   - 优先 support-changing；
   - weak replacement cap 更严格；
   - same-mask replacement 只有 true_rc 很强才保留。
6. 所有 worker returned journeys 仍必须 true-RC negative。
7. worker no-column / incomplete / duplicate-only 仍不 certificate。

测试：
- 5-task small-fast 不触发；
- 10-task hard-tail fingerprint 触发；
- 20-task hard-tail fingerprint 触发；
- previous success lowers threshold；
- repeated failure activates cooldown；
- support-aware filter reduces weak replacement；
- true-RC filter keeps all returned journeys negative；
- worker no-column still no certificate；
- scale-specific gate works.

跑 smoke：
- baseline
- current_probe old
- current_probe tuned
- tuned with cooldown
- tuned scale-gated
对 5 / 10 / 20 矩阵。

报告：
BPC_future/logical_graph/run_reports/20260612_sharded_pulse_phase7p_worker_tuning_zh.md

如果 tuned worker 仍无 ROI：
- 停止 active worker 主线；
- 进入 Phase 7Q 或 Phase 7R 视数据选择。

============================================================
Phase 7P-alt：Column impact filter / active-support-aware return
============================================================

如果 Phase 7O 发现 worker 能加列但不减少 tail retry，执行本分支。

目标：
worker 不再只返回“最负 true-RC”，而是返回更可能改变 RMP active support 的列。

实现：
1. 在 worker harvest diagnostics 中区分：
   - new_task_set
   - active_support_changing
   - active_replacement
   - inactive_replacement
   - same_mask_replacement
2. 默认 worker return quota：
   - 至少 min_new_task_set columns；
   - 至少 min_support_changing columns；
   - weak replacement cap；
   - duplicate / forbidden 全过滤；
   - same task-set replacement 只有 true_rc <= strong_threshold 才返回。
3. 如果 harvest 只有 weak replacement：
   - worker 可选择不返回，避免无效 RMP 小步；
   - 记录 worker_empty_after_impact_filter。
4. 继续保证所有返回列 true-RC negative。

测试：
- weak replacement only -> no returned journeys / no certificate；
- new task-set negative -> returned；
- support-changing negative -> returned；
- active replacement under cap -> returned；
- forbidden duplicate -> filtered；
- all returned true-RC negative。

A/B：
- worker without impact filter
- worker with impact filter
看 objective_delta、dual_l1_delta、retry count。
必须保证 5/10 no-regression，20 有改善才算成功。

============================================================
Phase 7Q：Adaptive sharding follow-up
============================================================

如果 Pulse 仍经常 incomplete，但 worker/audit counters 有信号，执行本分支。

目标：
控制 hard shard 和 child explosion，尤其为 20-task 证明/找列服务，同时不能拖垮 5/10。

实现：
1. 根据 Phase 7K/7O ROI calibration，调整 refinement threshold：
   - min_recursions
   - min_expanded
   - max_children
   - prune_rate_floor
2. 增加 scale-specific refinement:
   - 5-task 默认不 refine；
   - 10-task hard-tail 才 refine；
   - 20-task 可 refine，但必须有 child cap。
3. 增加 hard-child logging：
   - hardest_child_id
   - hardest_child_expanded
   - hardest_child_recursions
   - hardest_child_prune_rate
   - hardest_child_best_rc
4. 如果 child shard 仍 incomplete：
   - 不 certificate；
   - 记录 candidate for resume；
   - 不立即递归多层 refine，除非完整 partition 且 cap 可控。
5. 不做 partial child partition certificate。

测试：
- 5-task no refine default；
- 10 hard-tail refine；
- 20 refine with cap；
- hard child logging；
- parent refined iff complete child partition；
- partial child cap -> no refine；
- incomplete child blocks certificate；
- negative child propagates。

============================================================
Phase 7R：Proof-closed resume
============================================================

只有在 Phase 7O/7Q 证明 shard proof progress 有重复浪费时才执行。

目标：
让 child shard proof progress 跨调用累积，但不能把 frontier snapshot 当 proof。

实现：
1. Cache key 包含：
   - instance/logical graph hash
   - node/branch hash
   - true dual hash
   - cut hash
   - forbidden signature hash
   - pricing config hash
   - shard id
   - schema_version
   - proof_version
2. 可缓存：
   - certified shard flag；
   - proof-closed prefix record；
   - dominance archive；
   - frontier snapshot only for resume, not proof；
   - negative pool only for same context。
3. 禁止：
   - cross-dual reuse reduced-cost pruning；
   - stale cut/branch/forbidden reuse；
   - harvest-mode skipped subtree as proof；
   - low-ROI incomplete as proof。
4. 若 cache mismatch：
   - invalidate and restart / incomplete；
   - never certificate.

测试：
- dual/cut/branch/forbidden/config/proof_version change invalidates；
- frontier snapshot not certificate；
- proof-closed record certifies only under same key；
- harvest-mode visited prefix cannot certify；
- low_roi incomplete cannot certify；
- resume after interruption continues without changing result vs fresh run。

============================================================
Phase 7S：Experimental certificate gate，极窄条件
============================================================

只有满足以下条件才考虑执行：
- Phase 7H/7L/7O 没有 critical disagreement；
- very_small / 5-task no-wait 上 Pulse 和 legacy certified 稳定一致；
- Pulse incomplete 率低；
- no harvest mode；
- no duplicate-only；
- no unsupported branch；
- all shards certified；
- no timeout / recursion limit；
- cut context 要么 absent，要么 bound pruning fail-open 且 leaf true-RC 完整；
- explicit config enabled；
- 5/10 no-regression 已稳定；
- 20 improvement 不依赖伪证书。

新增配置：
journey_sharded_pulse_experimental_certificate_enabled: false

第一阶段只允许：
- very_small / test*
- 可选 Apollo5 / Tranq5，但必须显式 allow_small_instance_pulse_certificate=True
- task_waiting_allowed=False
- root node
- Ryan-Foster same/separate only or no branch
- no unsupported cuts

测试：
- all shards certified -> certificate only under explicit gate；
- gate off -> incomplete/audit only；
- waiting-allowed -> no certificate；
- harvest mode -> no certificate；
- timeout -> no certificate；
- duplicate-only -> no certificate；
- unsupported cut/branch -> no certificate；
- legacy agreement smoke。

不要直接放开 10/20 official certificate。

============================================================
Phase 7T：最终 A/B 与达标标准
============================================================

当 worker tuning 或 resume/certificate gate 有正向信号后，跑正式 A/B。

实例：
A. 5-task 全量：
- 所有 Apollo / Tranquillitatis 5-task 实例；
- 若没有全量，至少当前生成集中的所有 5-task。
B. 10-task hard set：
- tranq10_09
- tranq10_04
- tranq10_01
- tranq10_06
- apollo10_04
- apollo10_09
- 以及 Apollo10/Tranquillitatis10 代表集。
C. 20-task hard set：
- tranq20_01
- 至少再 2-5 个 20-task hard instances；
- Apollo20 先做 feasible single-task preflight，不可行实例单独分类。

profiles：
1. baseline
2. audit-only
3. tuned hidden-negative worker
4. tuned worker + impact filter
5. tuned worker + scale/hard-tail gate
6. tuned worker + resume if implemented
7. experimental certificate only on allowed tiny/5 if implemented

达标标准：
5-task：
- OPTIMAL count 不下降；
- objective / dual bound 一致；
- average time 增加 <= 5% 或 <= 0.2s；
- max time 不明显增加；
- no critical disagreement；
- no false certificate。

10-task：
- OPTIMAL count 不下降；
- TIME_LIMIT count 不增加；
- median wall time 不增加；
- hard-tail metrics 至少一个改善：
  - completion-bound retry count/time 下降；
  - hidden_negative_audit_count 下降；
  - final_judge max single-call time 下降；
  - gap 改善；
  - wall time 改善。

20-task：
- 至少 selected hard set 上明显改善：
  - average wall time 下降 >= 20%，或
  - same time limit 下 average gap 下降 >= 15%，或
  - completion-bound retry time 下降 >= 30%，或
  - OPTIMAL count 上升；
- 不能以减少证明导致错误 lower bound 的方式达成；
- no unbounded final judge；
- no critical disagreement。

如果达不到：
- 不要默认启用；
- 输出 negative-result report；
- 标记 Pulse path remains experimental。

============================================================
2026-06-13 Phase 8G 当前结论
============================================================

Phase 8G 已完成 residual profile-mask visibility 诊断：

- 新增 opt-in `--profile-mask-diagnostics`；
- 默认 benchmark 行为不变；
- `journey_pricing` JSONL 与 ROI summary 可以观察：
  - profile task-set samples；
  - reachable task-set samples；
  - negative task-set samples；
  - selected task-set samples。

Apollo20 `mt20_greedy_apollo_01` narrow probe 结论：

- follow-up first negative 仍为 `5,8,15`；
- 它不是 profile-DP top overloaded mask 的 exact hit；
- 但在 cap1000 和 cap5000 下，它都是：
  - reachable exact；
  - negative exact；
  - selected exact。

因此当前 residual tail 不是简单的“profile-DP 没看到该 task set”，也不是“只 materialize top overloaded masks”能解决。

下一步主线应转向：

- Phase 8H：selected negative materialization / return path attribution。

Phase 8H 只回答：

`5,8,15` 已经进入 selected negative mask，为什么 ordinary follow-up pricing 没把它作为 returned JourneyColumn 加回 RMP？

优先检查：

- selected candidate materialization cap；
- true-RC materialization scan order；
- duplicate / forbidden / dominated task-set filter；
- weak true-RC filter；
- selected profile 到 JourneyColumn 的 infeasible materialization 原因。

仍然不要做：

- production worker 默认开启；
- official certificate gate；
- resume；
- parallel；
- 20/100 A/B；
- top-mask-only materialization 主线。

============================================================
2026-06-13 Phase 8H 当前结论
============================================================

Phase 8H 已完成 selected negative materialization / return path 诊断：

- 新增 selected-candidate 输入 / 扫描 / 物化 / 返回 / 过滤计数；
- 新增 selected materialized / returned / unmaterialized / weak-filtered / filtered task-set samples；
- 这些字段只读记录，不改变 profile-DP、materialization、RMP insertion、worker trigger 或 certificate。

Apollo20 `mt20_greedy_apollo_01` narrow probe 结论：

- worker 仍只加入 `[6,19]`；
- ordinary follow-up first negative 仍是 `5,8,15`；
- `5,8,15` 是：
  - selected exact；
  - materialized exact；
  - returned exact；
  - 不是 unmaterialized；
  - 不是 weak-filtered；
  - 不是 post-materialization filtered。

因此 Phase 8G 的“selected 后为什么没返回”假设已被证伪。

正确结论是：

- ordinary follow-up 没有丢 `5,8,15`；
- 它确实返回并随后通过正常 add-column path 加入 pool；
- 当前 ROI 缺口仍是 Pulse worker 加入 `[6,19]` 后没有覆盖 / 消除 residual family `[5,8,15]`。

下一步主线应转向：

- Phase 8I：worker-vs-ordinary candidate family contrast。

Phase 8I 只比较同一 hard-tail context 中：

- Pulse worker returned `[6,19]`；
- ordinary follow-up returned `[5,8,15]`；

差异来自哪里：

- task-set ordering；
- start-time / arc-option domain；
- Pulse transition DFS deadline；
- profile-DP rough objective vs true-RC ranking；
- worker add-column 后 RMP dual context 是否仍保留 residual negative。

仍然不要做：

- production worker 默认开启；
- official certificate gate；
- resume；
- parallel；
- 20/100 A/B；
- 继续扩大 worker time limit；
- 把 ordinary materialization path 当作主要问题。

============================================================
2026-06-13 Phase 8I 当前结论
============================================================

Phase 8I 已完成 worker-vs-ordinary candidate family contrast：

- ROI summary 新增 `worker_vs_ordinary_*` 和 `pulse_worker_vs_ordinary_*` 只读字段；
- 这些字段直接记录 worker 首个 added task-set、worker 后 ordinary follow-up 首个 negative task-set、overlap、jaccard、relation、disjoint flag 和 contrast class；
- 不改变 Pulse worker、ordinary pricing、materialization、RMP insertion、certificate 或 lower-bound 语义。

Apollo20 `mt20_greedy_apollo_01` narrow probe 结论：

- worker 首个加入 task-set：`6,19`；
- ordinary follow-up 首个 negative task-set：`5,8,15`；
- overlap = `0`；
- jaccard = `0.0`；
- relation = `disjoint_task_set`；
- contrast class = `disjoint_residual_after_worker`；
- ordinary follow-up 的 `5,8,15` 仍是 selected exact、materialized exact、returned exact。

因此当前缺口已经更明确：

- 不是 ordinary selected materialization / return path 丢列；
- 不是 worker add-column path 无效；
- 而是 worker 首列 `[6,19]` 与随后 residual negative family `[5,8,15]` 完全 disjoint；
- worker 的首个 true-RC negative column 没有覆盖 / 消除 hard-tail residual family。

下一步主线应转向：

- Phase 8J：worker 内部候选族覆盖 / stop-after-first-negative 诊断。

Phase 8J 只回答：

- 同一 worker context 中，除了 `[6,19]`，是否也存在 `[5,8,15]` 或相近 residual family；
- 当前 `stop_after_first_negative` / impact filter / task ordering 是否让 worker 过早停在 inactive disjoint column；
- worker 若多返回少量候选，是否能覆盖 residual family。

仍然不要做：

- production worker 默认开启；
- official certificate gate；
- resume；
- parallel；
- 20/100 A/B；
- 简单扩大 worker time limit；
- 把这个结果解释为 production ROI 已成立。

============================================================
2026-06-13 Phase 8J 当前结论
============================================================

Phase 8J 已完成 worker 内部候选族覆盖诊断：

- `JourneyPricingResult` 新增 Pulse worker 内部候选池样本字段；
- JSONL 和 ROI summary 现在能记录 worker negative pool、harvested pool、returned-candidate pool 的 task-set / sequence / signature 样本；
- ROI summary 新增 `worker_vs_ordinary_negative_pool_*`、`worker_vs_ordinary_harvested_*`、`worker_vs_ordinary_returned_candidate_*` 及对应 `pulse_worker_vs_ordinary_*` aliases；
- 这些字段只读，不改变 worker trigger、search、filter、RMP insertion、certificate 或 lower-bound 语义。

Apollo20 `mt20_greedy_apollo_01` narrow probe 结论：

- worker 首个加入 task-set：`6,19`；
- ordinary follow-up 首个 negative task-set：`5,8,15`；
- worker negative pool samples：`[[6,19]]`；
- worker harvested samples：`[[6,19]]`；
- worker returned-candidate samples：`[[6,19]]`；
- `5,8,15` 对上述三个池子的 exact hit 全部为 `False`；
- overlap = `0`，jaccard = `0.0`；
- ordinary follow-up 的 `5,8,15` 仍在 profile reachable / negative / selected / materialized / returned 样本中 exact hit；
- critical disagreement = `False`。

因此 Phase 8J 证伪了：

- worker 已经看到 `5,8,15`，只是 impact filter 丢掉；
- worker 已经 harvest 到 `5,8,15`，只是 returned-candidate path 没选；
- worker returned-candidate pool 包含 residual family 但没有加列。

正确结论是：

- 在本次 worker call 中，Pulse 内部候选族只覆盖到 `[6,19]`；
- ordinary follow-up 的 residual family `[5,8,15]` 不在 worker negative pool / harvested pool / returned-candidate pool 中；
- 当前 ROI 缺口更可能来自 worker 内部搜索覆盖、stop-after-first-negative、shard/order/deadline、或 worker context 与 ordinary follow-up context 差异。

下一步主线应转向：

- Phase 8K：same-context residual reachability / stop-after-first-negative 诊断。

Phase 8K 只回答：

- 在同一 worker dual / cuts / pool / forbidden context 下，若不在首个 negative 后停止，是否能 reach / select / materialize `5,8,15`；
- 若不能，差异来自 shard coverage、task ordering、arc-option ordering、deadline，还是 context drift；
- 若能，再评估多返回少量候选是否能覆盖 residual family 并产生 ROI。

仍然不要做：

- production worker 默认开启；
- official certificate gate；
- resume；
- parallel；
- 20/100 A/B；
- 简单扩大 worker time limit；
- 只因为 8J 字段更完整就宣称 worker ROI 成立。

============================================================
2026-06-13 Phase 8K 当前结论
============================================================

Phase 8K 已完成 same-context residual reachability 诊断：

- ROI summary 新增 `worker_target_*` 和 `pulse_worker_target_*` 只读字段；
- 这些字段把 worker target sequence 的 task-set 与 worker negative pool / harvested pool / returned-candidate pool 做 overlap/exact 对比；
- 不改变 Pulse DFS、worker trigger、impact filter、RMP insertion、certificate 或 lower-bound 语义。

Apollo20 `mt20_greedy_apollo_01` narrow probe 覆盖 5 个 profile：

- `coverage_scan`
- `coverage_no_roi_gate`
- `coverage_target_priority`
- `coverage_target_transition_priority`
- `coverage_target_arc_option_priority`

关键结果：

- 基础 `coverage_scan`：
  - `stop_after_first_negative=False`；
  - worker returned / added = `3 / 3`；
  - returned candidates = `[7,19]`, `[6,19]`, `[11,12]`；
  - target task-set `5,8,15` exact = `False`；
  - target prefix = `0`；
  - blocked reason = `deadline`。

- `coverage_no_roi_gate`：
  - 结论与基础 scan 一致；
  - 说明不是 shard ROI gate 单独导致 target family 缺失。

- `coverage_target_priority`：
  - 只提升 target first-task shard；
  - worker returned / added = `8 / 8`；
  - returned candidates 中包含 exact target task-set `[5,8,15]`；
  - returned sequence sample 包含 `8,5,15`；
  - target exact sequence `8,15,5` 未完成，blocked reason = `deadline`。

- `coverage_target_transition_priority`：
  - 优先 exact transition `8 -> 15`；
  - target prefix 到 `2`；
  - 后续被 `time_window` 阻断；
  - returned candidates 包含 `[8,15]`, `[4,8,15]`, `[8,15,18]`，但没有 exact `[5,8,15]`。

- `coverage_target_arc_option_priority`：
  - 当前小预算下没有返回列；
  - target prefix = `1`；
  - blocked reason = `deadline`。

因此 Phase 8K 的结论是：

- residual task-set family `[5,8,15]` 不是不可达；
- 基础 worker 没看到它，主要因为 first-task shard ordering / budget 没进入 target family；
- 关闭 `stop_after_first_negative` 不足以解决；
- 强行追 exact ordinary sequence `8,15,5` 可能被 time-window/arc/start 域挡住；
- 更可行的是 residual-aware first-task shard scheduling，而不是强行固定 exact sequence。

下一步主线应转向：

- Phase 8L：residual-aware shard scheduling / first-task priority ROI gate。

Phase 8L 只允许在极窄条件下试验：

- hard-tail fingerprint；
- previous residual evidence；
- current-context hash 一致；
- small max-columns；
- no certificate effect；
- 5/10 no-regression 必须继续报告。

Phase 8L 要回答：

- 把 worker 引导到 residual first-task family 后，是否能减少 follow-up negative tail；
- `[5,8,15]` 这类 residual family 是否比 `[6,19]` 更有 RMP objective / dual movement；
- residual-aware scheduling 是否只是增加 inactive columns。

仍然不要做：

- production worker 默认开启；
- official certificate gate；
- resume；
- parallel；
- 20/100 A/B；
- 简单扩大 worker time limit；
- 把 exact sequence priority 当成主线。

============================================================
2026-06-13 Phase 8L 当前结论
============================================================

Phase 8L 已完成 residual-aware first-task priority ROI gate smoke：

- 新增 profile：
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_residual_target_priority_roi_gate`
- 该 profile 是 20-task only；
- 5/10 task 下直接 no-op；
- 20-task 下启用：
  - target first-task priority；
  - `stop_after_first_negative=False`；
  - same-iteration follow-up；
  - active-support continuation guard；
  - true-RC gate；
  - follow-up reserve；
  - failure cooldown；
  - current-probe hard-tail fingerprint。

跑了两层 smoke：

1. strict gated profile：
   - Apollo5 / Tranq5 / Apollo10 / Tranq10_09 / Apollo20；
   - worker 全部未触发；
   - official result unchanged；
   - no critical disagreement。

2. diagnostic no-gate target-priority 对照：
   - Apollo5 / Tranq5 / Apollo10 / Tranq10_09 / Apollo20；
   - 额外 Tranq20；
   - 5/10 全部 no-op / no-regression；
   - Apollo20 worker triggered，returned / added = `8 / 8`；
   - returned candidates 包含 exact `[5,8,15]`；
   - Apollo20 short-run primal 从 `921.640296` 改到 `857.401315`；
   - worker addition class = `changed_inactive_only`；
   - support-changing count = `0`；
   - follow-up 仍发现 residual negative `[4,12,18]`；
   - Tranq20 对 hardcoded Apollo residual target 无触发 / 无改善。

因此 Phase 8L 的结论是：

- strict gate 安全，但当前太保守，无法产生 ROI；
- target first-task priority 在 Apollo20 上有局部正信号；
- 但 hardcoded residual target 不泛化；
- worker 加列仍主要是 inactive-only；
- follow-up residual negative 仍存在；
- 还不能宣称 production ROI 成立。

下一步主线应转向：

- Phase 8M：automatic residual target extraction / residual-aware scheduling。

Phase 8M 要回答：

- 能否从 ordinary follow-up / previous diagnostic evidence 自动提取 residual target；
- 能否在 context hash 匹配时把 target first-task priority 自动配置给 worker；
- 是否能减少 follow-up residual negatives；
- 是否能产生 active-support-changing 或更强 RMP objective / dual movement；
- 是否继续保持 5/10 no-regression 和 no critical disagreement。

仍然不要做：

- production worker 默认开启；
- official certificate gate；
- resume；
- parallel；
- 20/100 A/B；
- 简单扩大 worker time limit；
- hardcode `8,15,5` 作为通用策略；
- 把 Apollo20 单点诊断正信号当作最终 ROI。

============================================================
2026-06-13 Phase 8M 当前结论
============================================================

Phase 8M 已完成 automatic residual target extraction：

- 新增 profiles：
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_auto_residual_target_diagnostic`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_auto_residual_target_roi_gate`
- auto target 只从同一实例的前序 row 提取；
- 只允许 20-task row；
- source row 必须有非空 worker context hash；
- 优先使用 `followup_first_negative_sequence`，否则使用 `followup_first_negative_task_set`；
- 不再 hardcode `8,15,5`；
- driver 增加 expected context hash guard：
  - context mismatch 时 skip worker；
  - skip reason = `residual_target_context_mismatch`。

Phase 8M smoke：

- Apollo5 / Tranq5 / Apollo10 / Tranq10_09：
  - auto target 未应用；
  - worker 未触发；
  - official result unchanged；
  - no critical disagreement。
- Apollo20 seed profile：
  - worker returned / added = `3 / 3`；
  - returned `[7,19]`, `[6,19]`, `[11,12]`；
  - follow-up residual negative = `[5,8,15]`。
- Apollo20 auto diagnostic：
  - 自动抽取 target sequence = `8,15,5`；
  - context match = True；
  - worker returned / added = `8 / 8`；
  - returned candidates 包含 exact `[5,8,15]`；
  - primal 从 baseline `921.640296` 到 `857.401315`；
  - addition class = `changed_inactive_only`；
  - active-support-changing count = `0`；
  - follow-up residual negative 仍存在，变成 `[4,12,18]`。
- Apollo20 strict auto ROI：
  - 自动抽取后 context match = True；
  - 但 hard-tail / max-iteration gate 下未触发 worker；
  - official result unchanged。

因此 Phase 8M 的结论是：

- residual target 自动提取机制成立；
- context hash guard 成立；
- diagnostic auto profile 能复现 manual target-priority 的 Apollo20 局部正信号；
- 但 ROI 仍未成立；
- 当前 worker 仍主要在 chase inactive-only residual columns；
- residual negative 不是消失，而是从 `[5,8,15]` 转移到 `[4,12,18]`；
- strict gate 仍太保守，不提供生产启用证据。

下一步主线应转向：

- Phase 8N：residual-chain source selection / active-support-aware target gate。

Phase 8N 要回答：

- 自动 residual target 是否只应从 active-support 相关 residual source 中提取；
- 是否能避免 inactive-only residual chasing；
- 是否能让 added_support_changing_count > 0；
- 是否能减少 follow-up residual negative，而不是只把 residual family 推到下一个 task-set；
- 是否继续保持 5/10 no-regression、no critical disagreement、no certificate effect。

仍然不要做：

- production worker 默认开启；
- official certificate gate；
- resume；
- parallel；
- 20/100 A/B；
- 简单扩大 worker time limit；
- 把 auto diagnostic 的 Apollo20 单点改善当作生产 ROI。

============================================================
2026-06-13 Phase 8N 当前结论
============================================================

Phase 8N 已完成 active-support-aware residual source gate：

- 新增 profiles：
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_auto_active_residual_target_diagnostic`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_auto_active_residual_target_roi_gate`
- active auto profile 仍是 20-task only；
- 5/10 task 下 no-op；
- source row 必须：
  - 来自同一实例；
  - 有非空 worker context hash；
  - 有 active-support-changing signal；
  - residual target 与 worker-added family 的关系为 `same_task_set` 或 `overlapping_task_set`；
- 如果 relation 是 `disjoint_task_set`，则拒绝 target；
- target 被拒绝时不 fallback 成 ordinary untargeted worker。

Phase 8N smoke：

- Apollo5 / Tranq5 / Apollo10 / Tranq10_09：
  - active auto target 未应用；
  - worker 未触发；
  - official result unchanged；
  - no critical disagreement。
- Apollo20 seed profile：
  - worker returned / added = `3 / 3`；
  - worker added_support_changing_count = `1`；
  - follow-up residual negative = `[5,8,15]`；
  - worker-vs-ordinary relation = `disjoint_task_set`。
- Apollo20 active auto profiles：
  - 检测到 candidate sequence = `8,15,5`；
  - source gate = `active_support`；
  - source gate reason = `residual_disjoint_from_worker`；
  - auto target applied = False；
  - worker_triggered = False；
  - official result unchanged；
  - no critical disagreement。

因此 Phase 8N 的结论是：

- 8M 的 residual candidate `[5,8,15]` 可自动识别；
- 但它与上一轮 worker family 是 disjoint；
- 盲目追它会复现 inactive-only residual chasing；
- active-source gate 正确拒绝了该 source；
- 当前仍未证明 worker ROI；
- 下一步应寻找真正 same/overlapping residual source，而不是继续扩大 worker 预算。

下一步主线应转向：

- Phase 8O：active residual source search / seed matrix。

Phase 8O 要回答：

- 在 Apollo20 / Tranq20 hard-tail 短矩阵中，是否存在 `same_task_set` 或 `overlapping_task_set` 的 residual source；
- active auto target 在这些 source 上是否能真正应用；
- 是否能产生 `added_support_changing_count > 0`；
- 是否能减少 follow-up residual negative，而不是只阻止所有 target；
- 是否继续保持 5/10 no-regression、no critical disagreement、no certificate effect。

仍然不要做：

- production worker 默认开启；
- official certificate gate；
- resume；
- parallel；
- 20/100 A/B；
- 简单扩大 worker time limit；
- 把 blocked target 当作 ROI 失败的最终结论。

============================================================
2026-06-13 Phase 8O 当前结论
============================================================

Phase 8O 已完成 active residual source search / seed matrix 的第一版：

- 新增 profile group：
  - `phase8o_active_source_search`
- 该 group 展开为：
  - `baseline`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_coverage_scan`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_coverage_no_roi_gate`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_auto_active_residual_target_diagnostic`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_auto_active_residual_target_roi_gate`
- summary 新增 row-level active source 字段：
  - `active_residual_source_candidate`
  - `active_residual_source_candidate_sequence`
  - `active_residual_source_context_hash`
  - `active_residual_source_relation`
  - `active_residual_source_active_signal_count`
  - `active_residual_source_gate_reason`
  - `active_residual_source_passed`
- auto-active rows 新增 previous-row source-search 字段：
  - `active_residual_source_search_candidate_count`
  - `active_residual_source_search_passed_count`
  - `active_residual_source_search_blocked_count`
  - `active_residual_source_search_blocked_disjoint_count`
  - `active_residual_source_search_blocked_no_active_count`
  - `active_residual_source_search_blocked_relation_count`
  - `active_residual_source_search_first_passed_*`
  - `active_residual_source_search_first_blocked_*`

Phase 8O smoke：

- 输出目录：
  - `BPC_future/results/sharded_pulse_phase8o_active_source_search_smoke_20260613`
- 实例：
  - Apollo5
  - Tranq5
  - Apollo10
  - tranq10_09
  - Apollo20 greedy-anchor
  - Tranq20 greedy-anchor
- 结果：
  - 5/10 全部 no-op；
  - 5/10 official result unchanged；
  - no critical disagreement；
  - Apollo20 两个 seed row 均有 active-support signal；
  - 但 residual relation 仍是 `disjoint_task_set`；
  - auto-active rows 统计为 `candidate=2 / passed=0 / blocked_disjoint=2`；
  - Tranq20 在同一短预算下没有产生 candidate source；
  - active auto target 没有应用；
  - worker 没有 fallback 成 untargeted worker。

因此 Phase 8O 的当前结论是：

- source-search 机制能显式区分：
  - 没有 source；
  - 有 source 但 disjoint blocked；
  - 有 passed same/overlapping source；
- 当前短矩阵没有发现 same/overlapping active residual source；
- 这不是 correctness blocker；
- 也不是 worker ROI 证明；
- 它说明继续 chase 8M/8N 的 disjoint residual target 没有依据。

下一步主线建议：

- Phase 8P：扩大但仍受控的 source-search seed matrix，或正式 pivot 到 non-Pulse-worker 路线。

Phase 8P 要回答：

- 是否能通过更多 20-task hard seeds / ordering variants 找到 same/overlapping active source；
- 如果连续两轮 source-search 仍无 passed source，是否满足 negative-result 条件 B 中“worker 加列不降低 tail / 20-task 无明显改善”的证据；
- 是否应停止 Pulse active-worker 主线，转向：
  - legacy/profile-DP proof-tail structural control；
  - RMP stabilization；
  - active fractional degeneracy；
  - 或 proof-closed resume。

仍然不要做：

- production worker 默认开启；
- official certificate gate；
- resume；
- parallel；
- 20/100 A/B；
- 简单扩大 worker time limit；
- 把 disjoint residual target 当作 active-source target。

============================================================
2026-06-13 Phase 8P 当前结论
============================================================

Phase 8P 已完成 expanded active source seed matrix 的第一版：

- 新增 instance group：
  - `phase8p_20_source_seed_matrix`
- 该 group 默认包含：
  - `mt20_greedy_apollo_01`
  - `mt20_greedy_tranq_01`
  - `tranq20_01`
- 未把旧 `apollo20_01` 放入默认 group：
  - 初次 smoke 时该 preset 在 initial single-task grid preflight 处失败；
  - 报错为 task 17 没有 feasible single-task timed trip；
  - 按目标文档，这属于 instance/grid feasibility 问题，不计为算法失败。
- 新增 profile group：
  - `phase8p_active_source_seed_matrix`
- 该 group 展开为：
  - `baseline`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_cooldown_ordered`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_coverage_scan`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_coverage_no_roi_gate`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_coverage_target_priority`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_auto_active_residual_target_diagnostic`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_auto_active_residual_target_roi_gate`
- summary 新增 source-search outcome：
  - `active_residual_source_search_outcome_class`
  - `active_residual_source_search_recommendation`

Phase 8P smoke：

- 输出目录：
  - `BPC_future/results/sharded_pulse_phase8p_active_source_seed_matrix_smoke_v2_20260613`
- 实例：
  - Apollo5
  - Tranq5
  - Apollo10
  - tranq10_09
  - Apollo20 greedy-anchor
  - Tranq20 greedy-anchor
  - tranq20_01
- 结果：
  - 5/10 全部 no-op；
  - 5/10 official result unchanged；
  - no critical disagreement；
  - Apollo20 greedy-anchor 找到一个 passed source：
    - source profile = `coverage_target_priority`
    - residual candidate = `12,4,18`
    - relation = `overlapping_task_set`
    - source-search outcome = `passed_source_available`
  - Apollo20 auto-active diagnostic：
    - 自动应用 target `12,4,18`
    - worker triggered
    - added journeys = 1
    - added_support_changing_count = 1
    - addition class = `active_replacement_task_set`
    - 但 follow-up residual negative 又回到 `8,15,5`
    - relation = `disjoint_task_set`
  - Apollo20 strict auto-active ROI：
    - 自动应用 target `12,4,18`
    - 但 worker 未触发
    - skip reason = `max_cg_iter_exceeded`
  - Tranq greedy / tranq20_01 没有 passed active source。

因此 Phase 8P 的当前结论是：

- 8O 的 “no passed source” 不是最终负结论；
- 扩大 seed matrix 后确实能找到 same/overlapping active residual source；
- 但即使应用该 source，当前 worker 仍没有稳定 ROI：
  - 只加了 1 个 active-replacement 列；
  - follow-up residual tail 没消失；
  - residual family 又切回 disjoint `[8,15,5]`；
  - strict ROI gate 下仍不触发。
- 这仍不足以打开 production worker 或 official certificate gate。

下一步主线建议：

- Phase 8Q：passed-source ROI validation / negative-result decision。

Phase 8Q 要回答：

- passed source `12,4,18` 是否能在重复 seed / 稍长但仍受控的预算下稳定产生 tail 改善；
- active-replacement 是否能转化为：
  - fewer follow-up negatives；
  - lower completion retry time；
  - lower final judge tail；
  - better primal / gap under same budget；
- 如果不能，是否满足条件 B 中 “worker 加列不降低 tail / 20-task 无明显改善” 的证据，停止继续扩大 Pulse active-worker 主线。

仍然不要做：

- production worker 默认开启；
- official certificate gate；
- resume；
- parallel；
- 20/100 A/B；
- 简单扩大 worker time limit；
- 把一个 passed-source diagnostic 当成 ROI 成立。

============================================================
2026-06-13 Phase 8Q 当前结论
============================================================

Phase 8Q 已完成 passed-source ROI validation：

- 新增 validation profiles：
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_auto_active_residual_target_validation_diagnostic`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_auto_active_residual_target_validation_roi_gate`
- 新增 profile group：
  - `phase8q_passed_source_roi_validation`
- 该 group 展开为：
  - `baseline`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_coverage_target_priority`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_auto_active_residual_target_diagnostic`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_auto_active_residual_target_validation_diagnostic`
  - `strict_worker_delayed_current_probe_impact_20_only_pre_heuristic_auto_active_residual_target_validation_roi_gate`

Phase 8Q smoke：

- 输出目录：
  - `BPC_future/results/sharded_pulse_phase8q_passed_source_roi_validation_smoke_20260613`
- 实例：
  - Apollo5
  - Tranq5
  - Apollo10
  - tranq10_09
  - Apollo20 greedy-anchor
  - Tranq20 greedy-anchor
  - tranq20_01
- 预算：
  - `time_limit=1.8`
  - `current_probe_time_limit=0.8`
  - `max_cg_iterations=4`
- 结果：
  - 5/10 全部 no-op；
  - 5/10 official result unchanged；
  - no critical disagreement；
  - Apollo20 coverage-target seed 产生 passed source `12,4,18`；
  - auto-active diagnostic 应用 `12,4,18`：
    - added journeys = 1
    - added_support_changing_count = 1
    - addition class = `active_replacement_task_set`
    - follow-up residual negative = `8,15,5`
    - relation = `disjoint_task_set`
  - validation diagnostic 再次应用同一个 target `12,4,18`：
    - 行为重复；
    - added journeys = 1
    - added_support_changing_count = 1
    - follow-up residual negative 仍是 `8,15,5`
  - validation ROI-gate profile 也提取到 `12,4,18`，但 worker 未触发：
    - skip reason = `max_cg_iter_exceeded`
  - 在该 1.8s 预算下，Apollo20 auto-active validation rows 的 primal 比 baseline 差，没有形成 tail 改善证据；
  - Tranq greedy / tranq20_01 没有 passed source。

因此 Phase 8Q 的当前结论是：

- passed source `12,4,18` 可重复应用；
- 但它没有稳定降低 tail；
- active-replacement columns 仍不能消除 disjoint residual negative；
- strict ROI gate 仍然不触发；
- 当前不支持继续扩大 Pulse active-worker 主线；
- 更不支持 production worker 或 official certificate gate。

下一步主线建议：

- Phase 8R：Pulse active-worker negative-result / pivot report。

Phase 8R 要回答：

- 是否已经满足条件 B 的主要证据：
  - 连续多轮 worker / source-search / validation 没有稳定 ROI；
  - worker 加列不降低 residual tail；
  - 20-task 没明显改善；
  - 5/10 no-regression 主要来自 worker no-op gate，而不是 worker 正收益；
- 是否停止继续扩大 Pulse active-worker；
- 后续转向哪个 non-worker 主线：
  - legacy/profile-DP proof-tail structural control；
  - RMP stabilization；
  - active fractional degeneracy；
  - proof-closed resume；
  - 或 direct final judge micro-optimization。

仍然不要做：

- production worker 默认开启；
- official certificate gate；
- resume；
- parallel；
- 20/100 A/B；
- 简单扩大 worker time limit；
- 为了让 worker 看起来有效而放松 ROI gate。

============================================================
2026-06-13 Phase 8R 当前结论
============================================================

Phase 8R 已完成 Pulse active-worker negative-result / pivot report：

- 新增报告：
  - `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase8r_active_worker_negative_result_pivot_zh.md`
- 本轮没有新增求解算法；
- 没有扩大 worker budget；
- 没有开启 production worker；
- 没有开启 official certificate gate；
- 没有做 resume / parallel。

Phase 8R 的核心判断：

- 停止继续扩大 `current-context Pulse hidden-negative active worker` 子路线；
- 不继续叠 active-worker trigger / cooldown / source / target / arc-option priority gates；
- 不增加 worker time limit；
- 不把 passed source `12,4,18` 当作 production ROI；
- 不把 Apollo20 单点可加列当作 20-task improvement 达标。

证据摘要：

- Phase 7O-7P 证明 worker 能安全加 true-RC negative columns，但 wall-time ROI 不稳定；
- Phase 7U-7Z 证明 worker-added columns 能进入 pool / support，但 follow-up residual negative tail 仍存在；
- Phase 7AA-7AG 证明 `[8,15,5]` residual family 不是 materialization / true-RC / signature mismatch，target-specific ordering 也没有转化为 ROI；
- Phase 8A-8F 证明当前主要信号是 residual disjoint negative / profile-DP tail / active fractional pressure，不是简单 pool duplicate pressure；
- Phase 8Q 证明 passed source `12,4,18` 可重复应用，但只产生 active-replacement column，follow-up residual 又回到 disjoint `8,15,5`。

对最终交付条件的当前状态：

- A 未满足：
  - 5/10 no-regression 主要靠 worker gate no-op；
  - 20-task 没有稳定 wall-time / gap / primal / tail 改善。
- B 对 active-worker 子路线已经满足主要负证据：
  - 连续多轮 A/B / source-search / validation 无稳定 ROI；
  - worker 加列不降低 residual tail；
  - selected 20-task 没明显改善。
- B 对完整 Pulse proof 路线尚未完全满足：
  - proof-closed resume 未实现；
  - refinement/resume 无降低 incomplete 还没有完整证据。
- C 未触发：
  - 当前无 critical disagreement；
  - 无 true-RC mismatch；
  - 无 signature/context mismatch。

因此：

- 不能宣布整个目标完成；
- 可以正式关闭 active-worker 扩张子路线；
- 下一步主线应转向 non-worker 方向。

下一步建议进入：

- Phase 9A：legacy/profile-DP proof-tail structural control；
- 重点诊断 ordinary heuristic 与 profile-DP / worker residual candidate family 的差异；
- 或并行准备 RMP stabilization / active fractional degeneracy 方向；
- proof-closed resume 只在需要补齐最终条件 B 的 proof-route 证据时再做。

============================================================
2026-06-13 Phase 9A 当前结论
============================================================

Phase 9A 已完成 profile-DP proof-tail bridge diagnostics：

- 新增 summary 字段：
  - `followup_proof_tail_bridge_class`
  - `followup_proof_tail_bridge_reason`
  - `pulse_worker_followup_proof_tail_bridge_class`
  - `pulse_worker_followup_proof_tail_bridge_reason`
- 新增只读分类 helper：
  - `_classify_followup_proof_tail_bridge()`
- 新增 profile group：
  - `phase9a_profile_dp_bridge_diagnostics`
- 新增报告：
  - `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase9a_profile_dp_bridge_diagnostics_zh.md`
- Smoke 输出：
  - `BPC_future/results/sharded_pulse_phase9a_profile_dp_bridge_diagnostics_smoke_20260613`

Phase 9A smoke：

- 实例：
  - Apollo5
  - Tranq5
  - Apollo10
  - tranq10_09
  - Apollo20 greedy-anchor
  - tranq20_01
- profile group：
  - baseline
  - coverage target priority
  - auto-active residual target validation diagnostic
- 结果：
  - 5/10 rows 全部 worker no-op；
  - 5/10 official result unchanged；
  - no critical disagreement；
  - 20-task opt-in worker rows 中，Apollo20 两个可观测 follow-up residual 均分类为 `profile_returned_residual_exact`；
  - 即这些 residual task set 已经被 profile-DP reachable / negative / selected / materialized / returned 精确覆盖；
  - Tranq20 没有 worker trigger，bridge class 为 `no_worker_add`。

因此 Phase 9A 的当前结论是：

- 当前 Apollo residual tail 主要不是 profile-DP selected / materialization / returned bridge 丢失；
- 不应优先补 selected-candidate materialization bridge；
- 不应重启 Pulse active-worker target-specific tuning；
- 下一步应诊断 profile-DP 已返回 residual 后，RMP / dual / active support / final judge tail 为什么仍不收敛。

下一步建议进入：

- Phase 9B：profile-DP returned residual tail sequence attribution。

Phase 9B 要回答：

- profile-DP returned residual 后，下一轮 RMP objective / dual movement 是否足够；
- residual task-set 是否反复换形再生；
- returned residual 是 active-support-changing 还是 weak replacement；
- rough RC / true RC 排序是否导致 stronger residual 被延后；
- final judge tail 是否发生在 residual returned 后的证明阶段。

仍然不要做：

- production worker 默认开启；
- official certificate gate；
- resume / parallel；
- 20/100 A/B；
- 简单加大 pricing cap 或 worker time limit。

============================================================
2026-06-13 Phase 9B 当前结论
============================================================

Phase 9B 已完成 profile-DP returned residual tail sequence attribution：

- 新增 summary 字段：
  - `followup_returned_residual_tail_class`
  - `followup_returned_residual_tail_reason`
  - `followup_negative_task_set_sequence`
  - `followup_negative_task_set_unique_count`
  - `followup_negative_task_set_repeat_count`
  - `followup_first_negative_addition_productivity_class`
  - `followup_first_negative_added_journeys`
  - `followup_first_negative_added_new_task_set_count`
  - `followup_first_negative_added_replacement_count`
  - `followup_first_negative_added_support_changing_count`
  - `followup_post_first_negative_rmp_objective_delta`
  - `followup_post_first_negative_dual_l1_delta`
  - 以及 `pulse_worker_followup_*` 对应字段。
- 新增只读 helper：
  - `_first_matching_column_addition_after()`
  - `_first_rmp_after_index()`
  - `_classify_returned_residual_tail()`
- 新增 profile group：
  - `phase9b_returned_residual_tail_attribution`
- 新增报告：
  - `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase9b_returned_residual_tail_attribution_zh.md`
- Smoke 输出：
  - `BPC_future/results/sharded_pulse_phase9b_returned_residual_tail_attribution_smoke_20260613`

Phase 9B smoke：

- 5/10 rows 全部 worker no-op；
- 5/10 official result unchanged；
- no critical disagreement；
- Apollo20 两个 opt-in worker rows 的 tail class 都是：
  - `returned_residual_then_new_negative_family`
- coverage-target row：
  - follow-up negative sequence = `4,12,18|5,12,16`
  - first follow-up residual addition = `changed_inactive_only`
  - first follow-up residual added new task-set count = 1
  - active-support-changing count = 0
  - post-first-negative objective delta = `-40.33852`
  - post-first-negative dual L1 delta = `400.061096`
- auto-active validation row：
  - follow-up negative sequence = `5,8,15|5,12,18|4,8,12`
  - first follow-up residual addition = `changed_inactive_only`
  - first follow-up residual added new task-set count = 1
  - active-support-changing count = 0
  - post-first-negative objective delta = `-139.913748`
  - post-first-negative dual L1 delta = `139.913748`

因此 Phase 9B 的当前结论是：

- profile-DP returned residual 后，不是同一 task-set 反复卡住；
- first follow-up residual 能作为 new task-set 加入；
- 但 first follow-up residual 不是 active-support-changing；
- 加入后 objective / dual 有移动；
- 后续仍继续出现新的 negative task-set family；
- 当前更像 residual-family regeneration / inactive-new-column tail / RMP-dual degeneracy，而不是 profile-DP 候选不可见。

============================================================
2026-06-13 Phase 9C：RMP residual active-support attribution
============================================================

本轮只做只读归因，不改变 solver 语义。

新增 summary 字段：

- `followup_first_negative_active_after_addition`
- `followup_first_negative_active_value_after_addition`
- `followup_first_negative_active_journey_count_after_addition`
- `followup_first_negative_active_relation_after_addition`
- `followup_active_fractional_ratio_after_first_negative`
- `followup_active_total_value_after_first_negative`
- `followup_active_task_set_hash_after_first_negative`
- `followup_rmp_residual_impact_class`
- `followup_rmp_residual_impact_reason`
- 以及 `pulse_worker_followup_*` 对应字段。

新增只读 helper：

- `_first_pool_after_index()`
- `_active_task_set_value_from_pool_record()`
- `_classify_rmp_residual_impact()`

新增 profile group：

- `phase9c_rmp_residual_active_support_attribution`

新增报告：

- `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase9c_rmp_residual_active_support_attribution_zh.md`

Smoke 输出：

- `BPC_future/results/sharded_pulse_phase9c_rmp_residual_active_support_attribution_smoke_20260613`

Phase 9C smoke：

- `apollo5`、`tranq5`、`apollo10`、`tranq10_09`：
  - worker 未触发；
  - official result 与 baseline 一致；
  - critical disagreement count = 0。
- `tranq20_01`：
  - 本轮短 smoke 中 worker 未触发；
  - official result 与 baseline 一致；
  - critical disagreement count = 0。
- `mt20_greedy_apollo_01` coverage-target row：
  - worker added journeys = 8；
  - worker added new task sets = 8；
  - worker added support-changing count = 0；
  - follow-up negative sequence = `4,12,18|5,12,16|12,16,17`；
  - first follow-up residual addition = `changed_inactive_only`；
  - first residual active after addition = `True`；
  - first residual active value after addition = `1.0`；
  - active relation = `same_task_set`；
  - impact class = `active_residual_then_new_negative_family`。
- `mt20_greedy_apollo_01` auto-active validation row：
  - worker added journeys = 1；
  - worker added new task sets = 0；
  - worker added support-changing count = 1；
  - follow-up negative sequence = `5,8,15|5,12,18|4,8,12`；
  - first follow-up residual addition = `changed_inactive_only`；
  - first residual active after addition = `True`；
  - first residual active value after addition = `1.0`；
  - active relation = `same_task_set`；
  - impact class = `active_residual_then_new_negative_family`。

因此 Phase 9C 的当前结论是：

- profile-DP 能看到并返回 residual；
- first follow-up residual 能作为 new task-set 加入；
- 加入后下一轮也可以进入 active support，且 observed value = `1.0`；
- 但 active residual 后仍继续出现新的 negative task-set family；
- 当前更像 residual-family regeneration / active-basis churn / RMP-dual degeneracy，而不是“首个 residual 加列后完全不活跃”。

下一步建议进入：

- Phase 9D：residual-family chain / active-value persistence / RMP-dual degeneracy attribution。

Phase 9D 要回答：

- active residual 的 value 是否能持续到后续 RMP；
- 后续 negative family 与 active residual 是否共享任务、资源或 dual shock；
- active basis 是否在多个 residual family 间切换；
- 该 tail 是否更适合转向 RMP stabilization / pool compression / legacy final judge optimization。

============================================================
2026-06-13 Phase 9D：Residual-family chain attribution
============================================================

本轮继续只做只读归因，不改变 solver 语义。

新增 summary 字段：

- `followup_first_negative_active_persistence_count`
- `followup_first_negative_active_value_sequence`
- `followup_first_negative_active_last_value`
- `followup_active_basis_hash_sequence_after_first_negative`
- `followup_active_basis_unique_count_after_first_negative`
- `followup_active_basis_churn_count_after_first_negative`
- `followup_negative_family_after_first_count`
- `followup_negative_family_after_first_relation_sequence`
- `followup_negative_family_after_first_disjoint_count`
- `followup_negative_family_after_first_overlapping_count`
- `followup_negative_family_after_first_same_count`
- `followup_negative_family_after_first_max_overlap`
- `followup_negative_family_after_first_max_jaccard`
- `followup_residual_family_chain_class`
- `followup_residual_family_chain_reason`
- 以及 `pulse_worker_followup_*` 对应字段。

新增只读 helper：

- `_pool_records_after_index()`
- `_active_residual_persistence_summary()`
- `_negative_family_after_first_summary()`
- `_classify_residual_family_chain()`

新增 profile group：

- `phase9d_residual_family_chain_attribution`

新增报告：

- `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase9d_residual_family_chain_attribution_zh.md`

Smoke 输出：

- `BPC_future/results/sharded_pulse_phase9d_residual_family_chain_attribution_smoke_20260613`

Phase 9D smoke：

- `apollo5`、`tranq5`、`apollo10`、`tranq10_09`：
  - worker 未触发；
  - official result 与 baseline 一致；
  - critical disagreement count = 0。
- `tranq20_01`：
  - 本轮短 smoke 中 worker 未触发；
  - official result 与 baseline 一致；
  - critical disagreement count = 0。
- `mt20_greedy_apollo_01` coverage-target row：
  - worker added journeys = 8；
  - worker added new task sets = 8；
  - worker added support-changing count = 0；
  - follow-up negative sequence = `4,12,18`；
  - first residual active value sequence = `[1.0]`；
  - active basis hash unique count = 1；
  - active basis churn count = 0；
  - residual family chain class = `active_residual_no_observed_new_family`。
- `mt20_greedy_apollo_01` auto-active validation row：
  - worker added journeys = 1；
  - worker added new task sets = 0；
  - worker added support-changing count = 1；
  - follow-up negative sequence = `5,8,15|5,12,18|4,8,12`；
  - first residual active value sequence = `[1.0,1.0]`；
  - first residual active last value = `1.0`；
  - active basis hash unique count = 1；
  - active basis churn count = 0；
  - later negative family relation sequence = `overlapping_task_set|overlapping_task_set`；
  - max overlap to first residual = 1；
  - max Jaccard to first residual = 0.2；
  - residual family chain class = `persistent_active_residual_with_overlapping_new_family`。

因此 Phase 9D 的当前结论是：

- 首个 residual 不只是短暂进入 active support；
- auto-active validation 中它跨两个后续 pool diagnostics 仍保持 active value = `1.0`；
- active basis hash 没有变化；
- 但同一 active basis 下仍出现两个弱重叠的新 negative family；
- 当前 tail 不需要 active basis hash churn 就能继续发生。

下一步建议进入：

- Phase 9E：RMP degeneracy / pool compression / active-family stabilization 诊断。

Phase 9E 要回答：

- 后续 negative family 是否与 pool duplicate pressure / active fractional pressure 更相关；
- active basis hash 稳定时，dual movement 和 new negative family 是否仍同步出现；
- 是否应从 Pulse worker 继续转向 RMP stabilization / pool compression / legacy final judge optimization；
- 只读诊断能否给出一个明确 pivot target。

============================================================
2026-06-13 Phase 9E：RMP degeneracy / pool-pressure attribution
============================================================

本轮继续只做只读归因，不改变 solver 语义。

新增 summary 字段：

- `followup_post_first_negative_pool_duplicate_task_sets`
- `followup_post_first_negative_pool_duplicate_task_set_ratio`
- `followup_post_first_negative_pool_active_duplicate_task_sets`
- `followup_post_first_negative_pool_active_duplicate_task_set_ratio`
- `followup_post_first_negative_pool_avg_journeys_per_task_set`
- `followup_post_first_negative_pool_max_journeys_per_task_set`
- `followup_post_first_negative_pool_active_avg_journeys_per_task_set`
- `followup_post_first_negative_pool_active_fractional_value_sum`
- `followup_post_first_negative_pool_active_fractional_value_max`
- `followup_post_first_negative_pool_active_fractional_value_min`
- `followup_post_first_negative_pool_active_fractional_small_value_count`
- `followup_rmp_degeneracy_pressure_class`
- `followup_rmp_degeneracy_pressure_reason`
- 以及 `pulse_worker_followup_*` 对应字段。

新增只读 helper：

- `_classify_rmp_degeneracy_pressure()`

新增 profile group：

- `phase9e_rmp_degeneracy_pool_pressure_attribution`

新增报告：

- `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase9e_rmp_degeneracy_pool_pressure_attribution_zh.md`

Smoke 输出：

- `BPC_future/results/sharded_pulse_phase9e_rmp_degeneracy_pool_pressure_attribution_smoke_20260613`

Phase 9E smoke：

- `apollo5`、`tranq5`、`apollo10`、`tranq10_09`：
  - worker 未触发；
  - official result 与 baseline 一致；
  - critical disagreement count = 0。
- `tranq20_01`：
  - 本轮短 smoke 中 worker 未触发；
  - official result 与 baseline 一致；
  - critical disagreement count = 0。
- `mt20_greedy_apollo_01` coverage-target row：
  - worker added journeys = 8；
  - worker added new task sets = 8；
  - worker added support-changing count = 0；
  - follow-up negative sequence = `4,12,18|5,12,16`；
  - residual family chain class = `persistent_active_residual_with_overlapping_new_family`；
  - post-first-negative objective delta = `-40.33852`；
  - post-first-negative dual L1 delta = `400.061096`；
  - pool duplicate ratio = `0.0`；
  - active duplicate ratio = `0.0`；
  - active fractional ratio after first negative = `0.583333333`；
  - active fractional value sum = `3.5`；
  - rmp degeneracy pressure class = `active_fractional_pressure`。
- `mt20_greedy_apollo_01` auto-active validation row：
  - worker added journeys = 1；
  - worker added new task sets = 0；
  - worker added support-changing count = 1；
  - follow-up negative sequence = `5,8,15|5,12,18`；
  - residual family chain class = `persistent_active_residual_with_overlapping_new_family`；
  - post-first-negative objective delta = `-139.913748`；
  - post-first-negative dual L1 delta = `139.913748`；
  - pool duplicate ratio = `0.0`；
  - active duplicate ratio = `0.0`；
  - active fractional ratio after first negative = `0.0`；
  - active fractional value sum = `0.0`；
  - active basis unique count = 1；
  - active basis churn count = 0；
  - rmp degeneracy pressure class = `stable_basis_overlapping_family_with_dual_move`。

因此 Phase 9E 的当前结论是：

- Apollo20 tail 不能简单归因于 duplicate task-set pressure；
- coverage-target row 更像 active fractional pressure；
- auto-active validation row 更像 stable active basis + dual movement + overlapping new family；
- 继续增加 Pulse worker budget / trigger 不合理；
- 下一步应把主线转向 RMP stabilization / active-family stabilization / pool compression / legacy final judge tail optimization 的最小诊断。

下一步建议进入：

- Phase 9F：RMP stabilization / pool compression 前置诊断。

Phase 9F 要回答：

- active fractional pressure row 是否存在可安全收缩或稳定的 active family；
- stable-basis overlapping-family row 是否由 dual movement / degeneracy 引起；
- 是否存在不改变 exactness 的 pool policy 或 RMP stabilization 方向；
- 如果只读诊断继续支持 negative result，准备把 Pulse worker/proof 路线的无 ROI 证据补全到最终 B 类交付。

============================================================
2026-06-13 Phase 9F：RMP stabilization / pool-compression diagnostics
============================================================

本轮继续只做只读归因，不改变 solver 语义。

新增 summary 字段：

- `followup_post_first_negative_dual_objective_abs_ratio`
- `followup_post_first_negative_dual_move_class`
- `followup_pool_compression_candidate_class`
- `followup_pool_compression_candidate_reason`
- `followup_rmp_stabilization_candidate_class`
- `followup_rmp_stabilization_candidate_reason`
- 以及 `pulse_worker_followup_*` 对应字段。

新增只读 helper：

- `_dual_objective_abs_ratio()`
- `_classify_dual_move()`
- `_classify_pool_compression_candidate()`
- `_classify_rmp_stabilization_candidate()`

新增 profile group：

- `phase9f_rmp_stabilization_pool_compression_diagnostics`

新增报告：

- `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase9f_rmp_stabilization_pool_compression_diagnostics_zh.md`

Smoke 输出：

- `BPC_future/results/sharded_pulse_phase9f_rmp_stabilization_pool_compression_diagnostics_smoke_20260613`

Phase 9F smoke：

- `apollo5`、`tranq5`、`apollo10`、`tranq10_09`：
  - worker 未触发；
  - official result 与 baseline 一致；
  - critical disagreement count = 0。
- `tranq20_01`：
  - 本轮短 smoke 中 worker 未触发；
  - official result 与 baseline 一致；
  - critical disagreement count = 0。
- `mt20_greedy_apollo_01` coverage-target row：
  - worker added journeys = 8；
  - worker added new task sets = 8；
  - worker added support-changing count = 0；
  - follow-up negative sequence = `4,12,18`；
  - rmp degeneracy pressure class = `active_fractional_pressure`；
  - active fractional ratio = `0.583333333`；
  - active fractional value sum = `3.5`；
  - post-first-negative objective delta = `-40.33852`；
  - post-first-negative dual L1 delta = `400.061096`；
  - dual/objective abs ratio = `9.917594795`；
  - dual move class = `large_dual_move_relative_to_objective`；
  - pool compression candidate class = `no_pool_compression_signal`；
  - rmp stabilization candidate class = `active_family_stabilization_candidate`。
- `mt20_greedy_apollo_01` auto-active validation row：
  - worker added journeys = 1；
  - worker added new task sets = 0；
  - worker added support-changing count = 1；
  - follow-up negative sequence = `5,8,15|5,12,18|4,8,12`；
  - rmp degeneracy pressure class = `stable_basis_overlapping_family_with_dual_move`；
  - post-first-negative objective delta = `-139.913748`；
  - post-first-negative dual L1 delta = `139.913748`；
  - dual/objective abs ratio = `1.0`；
  - dual move class = `proportional_dual_objective_move`；
  - pool compression candidate class = `no_pool_compression_signal`；
  - rmp stabilization candidate class = `stable_basis_dual_stabilization_candidate`。

因此 Phase 9F 的当前结论是：

- 当前证据不支持把下一步设成 pool compression policy；
- coverage-target row 更像 active-family stabilization candidate；
- auto-active validation row 更像 stable-basis dual stabilization candidate；
- 继续扩大 Pulse worker 仍然不合理；
- 下一步如果继续推进，应是极窄 opt-in 的 RMP/dual stabilization diagnostic，且不得影响 official certificate / lower bound。

下一步建议进入：

- Phase 9G：RMP/dual stabilization opt-in diagnostic design。

Phase 9G 要回答：

- 能否设计一个只影响 pricing/RMP探索、但不影响 official certificate 的 stabilization diagnostic；
- 是否能在 active-family / stable-basis 两类 row 上给出更直接的 no-ROI 或有-ROI 信号；
- 若仍无改善，准备把当前 Pulse worker/proof 路线归入最终 B 类交付的 negative-result 证据。

============================================================
2026-06-13 Phase 9G：RMP/dual stabilization diagnostic design
============================================================

本轮只做 opt-in diagnostic design，不启用新的求解策略。

新增 summary 字段：

- `followup_stabilization_diagnostic_design_class`
- `followup_stabilization_diagnostic_design_reason`
- `followup_stabilization_diagnostic_recommended_profile`
- `followup_stabilization_diagnostic_guarded_config_keys`
- `followup_stabilization_diagnostic_certificate_effect_allowed`
- 以及 `pulse_worker_followup_*` 对应字段。

新增只读 helper：

- `_stabilization_diagnostic_design()`

新增 profile group：

- `phase9g_rmp_dual_stabilization_diagnostic_design`

新增报告：

- `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase9g_rmp_dual_stabilization_diagnostic_design_zh.md`

Smoke 输出：

- `BPC_future/results/sharded_pulse_phase9g_rmp_dual_stabilization_diagnostic_design_smoke_20260613`

Phase 9G smoke：

- `apollo5`、`tranq5`、`apollo10`、`tranq10_09`：
  - worker 未触发；
  - official result 与 baseline 一致；
  - critical disagreement count = 0；
  - design class = `no_worker_add`。
- `tranq20_01`：
  - 本轮短 smoke 中 worker 未触发；
  - official result 与 baseline 一致；
  - critical disagreement count = 0。
- `mt20_greedy_apollo_01` coverage-target row：
  - rmp stabilization candidate class = `active_family_stabilization_candidate`；
  - design class = `active_family_stabilization_diagnostic`；
  - recommended profile = `diagnostic_active_family_dual_anchor_audit_only`；
  - certificate effect allowed = `False`；
  - guarded config keys 包含：
    - `journey_rmp_stabilization_diagnostic_enabled`
    - `journey_rmp_stabilization_diagnostic_mode`
    - `journey_rmp_stabilization_diagnostic_allow_certificate_effect`
    - `journey_rmp_stabilization_diagnostic_context_hash_required`
    - `journey_rmp_stabilization_active_family_fractional_threshold`
    - `journey_rmp_stabilization_active_family_dual_anchor_weight`
- `mt20_greedy_apollo_01` auto-active validation row：
  - rmp stabilization candidate class = `stable_basis_dual_stabilization_candidate`；
  - design class = `stable_basis_dual_stabilization_diagnostic`；
  - recommended profile = `diagnostic_stable_basis_dual_anchor_audit_only`；
  - certificate effect allowed = `False`；
  - guarded config keys 包含：
    - `journey_rmp_stabilization_diagnostic_enabled`
    - `journey_rmp_stabilization_diagnostic_mode`
    - `journey_rmp_stabilization_diagnostic_allow_certificate_effect`
    - `journey_rmp_stabilization_diagnostic_context_hash_required`
    - `journey_rmp_stabilization_stable_basis_dual_anchor_weight`
    - `journey_rmp_stabilization_stable_basis_required_hash_repeats`

因此 Phase 9G 的当前结论是：

- RMP/dual stabilization 只能作为下一轮 audit-only diagnostic；
- 必须 context-hash guarded；
- 必须 `certificate_effect_allowed=False`；
- 不允许影响 official lower bound；
- 不能替代 true-dual exact pricing certificate；
- 继续扩大 Pulse worker 仍然不合理。

下一步建议进入：

- Phase 9H：audit-only RMP/dual stabilization probe skeleton。

Phase 9H 要回答：

- 能否实现一个严格 no-official-effect 的 stabilization probe skeleton；
- 日志是否能记录 mode、context hash、anchor weight、candidate source、certificate_effect_allowed=False；
- 5/10 是否仍 no-regression；
- 20-task smoke 是否给出有意义的 stabilization 信号；
- 如果仍无改善，准备把 Pulse worker/proof 路线的无 ROI 证据补全到最终 B 类交付。

仍然不要做：

- production worker 默认开启；
- official certificate gate；
- resume / parallel；
- 20/100 A/B；
- 简单增大 worker 或 pricing time limit。

2026-06-13 Phase 9H：audit-only RMP/dual stabilization probe skeleton

本轮完成：

- 新增 profile group：
  - `phase9h_rmp_dual_stabilization_probe_skeleton`
- 新增 summary 字段：
  - `followup_stabilization_probe_enabled`
  - `followup_stabilization_probe_status`
  - `followup_stabilization_probe_reason`
  - `followup_stabilization_probe_mode`
  - `followup_stabilization_probe_candidate_source`
  - `followup_stabilization_probe_anchor_weight`
  - `followup_stabilization_probe_context_hash_required`
  - `followup_stabilization_probe_context_hash`
  - `followup_stabilization_probe_certificate_effect_allowed`
  - `followup_stabilization_probe_official_effect_allowed`
  - `followup_stabilization_probe_mutates_rmp`
  - `followup_stabilization_probe_design_profile`
  - 以及对应 `pulse_worker_followup_*` alias。
- 新增只读 helper：
  - `_stabilization_probe_skeleton()`
- 新增报告：
  - `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase9h_rmp_dual_stabilization_probe_skeleton_zh.md`
- Smoke 输出：
  - `BPC_future/results/sharded_pulse_phase9h_rmp_dual_stabilization_probe_skeleton_smoke_20260613`

Smoke 结果：

- `apollo5`、`tranq5`、`apollo10`、`tranq10_09`：
  - worker no-op；
  - probe enabled = `False`；
  - official result 与 baseline 一致；
  - critical disagreement = `False`。
- `tranq20_01`：
  - worker no-op；
  - probe enabled = `False`；
  - official result 与 baseline 一致；
  - critical disagreement = `False`。
- `mt20_greedy_apollo_01` coverage-target profile：
  - design class = `active_family_stabilization_diagnostic`；
  - probe mode = `active_family_dual_anchor`；
  - candidate source = `active_family_stabilization_candidate`；
  - anchor weight = `0.1`；
  - context hash 非空。
- `mt20_greedy_apollo_01` auto-active validation profile：
  - design class = `stable_basis_dual_stabilization_diagnostic`；
  - probe mode = `stable_basis_dual_anchor`；
  - candidate source = `stable_basis_dual_stabilization_candidate`；
  - anchor weight = `0.05`；
  - context hash 非空。

所有 probe plan 均固定：

- `certificate_effect_allowed=False`
- `official_effect_allowed=False`
- `mutates_rmp=False`

解释：

- Phase 9H 只是 probe skeleton / summary guard；
- 不启用 RMP stabilization；
- 不改变 solver、pricing、RMP、driver official decision；
- Apollo20 profile-level official-result 差异来自既有 worker profile path，不来自 probe skeleton；
- 该阶段仍不是性能 ROI 结论。

下一步如果继续，应进入更窄的 RMP/dual stabilization audit-only 实验；如果不继续，应把 Pulse worker/proof 路线无稳定 ROI 的证据整理成最终 B 类交付候选。仍然不要做 production worker 默认开启、official certificate gate、resume / parallel、20/100 A/B 或简单增大 worker/pricing time limit。

2026-06-13 Phase 9I：RMP/dual stabilization A/B smoke

本轮完成：

- 新增 experimental calibration profiles：
  - `experimental_l1_previous_dual_stabilization_20_only`
  - `experimental_l1_zero_dual_stabilization_20_only`
- 新增 profile group：
  - `phase9i_rmp_dual_stabilization_ab`
- 新增 summary 字段：
  - `dual_stabilization_events`
  - `dual_stabilization_accepted_count`
  - `dual_stabilization_skipped_count`
  - `dual_stabilization_status_sequence`
  - `dual_stabilization_source_sequence`
  - `dual_stabilization_mode_sequence`
  - `dual_stabilization_reference_sequence`
  - `dual_stabilization_first_accepted_cg_iter`
  - `dual_stabilization_current_pool_negative_count_max`
  - `dual_stabilization_objective_mismatch_count`
  - `dual_stabilization_current_pool_infeasible_count`
  - `dual_stabilization_time`
  - `dual_stabilization_effect_class`
- 新增 helper：
  - `_dual_stabilization_metrics()`
- 新增报告：
  - `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase9i_rmp_dual_stabilization_ab_zh.md`
- Smoke 输出：
  - `BPC_future/results/sharded_pulse_phase9i_rmp_dual_stabilization_ab_smoke_20260613`

实验 profile 边界：

- 仅 20-task 启用；
- 5/10 下 no-op；
- 不启用 Sharded Pulse audit / hidden-negative worker；
- 不改变 production default；
- `journey_dual_stabilization_certificate_candidate_enabled=False`；
- `journey_dual_stabilization_disable_on_certificate_candidate=True`；
- stabilized dual 仍依赖现有 objective-match 和 current-pool feasibility guard。

Smoke 结果：

- `apollo5`、`tranq5`、`apollo10`、`tranq10_09`：
  - `dual_stabilization_events=0`；
  - official result 与 baseline 一致；
  - critical disagreement = `False`；
  - improvement class = `no_regression`。
- `mt20_greedy_apollo_01`：
  - previous-anchor / zero-anchor 都 accepted stabilized dual 2 次；
  - `current_pool_negative_count_max=0`；
  - `objective_mismatch_count=0`；
  - `current_pool_infeasible_count=0`；
  - official result 与 baseline 一致；
  - 未观察到求解改善。
- `tranq20_01`：
  - previous-anchor / zero-anchor 都 accepted stabilized dual 4 次；
  - `current_pool_negative_count_max=0`；
  - `objective_mismatch_count=0`；
  - `current_pool_infeasible_count=0`；
  - previous-anchor：official pricing state 从 baseline `INCOMPLETE_LIMIT` 变为 `FOUND_NEGATIVE`，primal 不变；
  - zero-anchor：official pricing state 从 baseline `INCOMPLETE_LIMIT` 变为 `FOUND_NEGATIVE`，primal 从 `783.715884` 改善到 `781.398505`。

解释：

- Phase 9I 是短 smoke，不是最终 ROI 证明；
- Tranq20 zero-anchor 有正向信号，但还不能宣称 20-task selected hard set 明显改善；
- 当前更合理的下一步是 Phase 9J：重复/稍长的 zero-anchor vs previous-anchor A/B；
- 继续保持 5/10 no-regression；
- 不做 production default；
- 不做 official certificate gate；
- 不回到扩大 Pulse worker 主线。

Phase 9J 已完成：

- `run_sharded_pulse_roi_calibration.py` 新增 `--repeat-count`；
- summary 新增 `repeat_index`；
- baseline 对比按 `(instance, repeat_index)` 隔离；
- 多次 repeat 时 per-run JSONL 使用 `__r<repeat_index>` 后缀，避免覆盖；
- 新增 profile group：
  - `phase9j_rmp_dual_stabilization_repeat_ab`。

Smoke：

- 输出目录：
  - `BPC_future/results/sharded_pulse_phase9j_rmp_dual_stabilization_repeat_ab_smoke_20260613`
- 实例：
  - `apollo5`
  - `tranq5`
  - `apollo10`
  - `tranq10_09`
  - `mt20_greedy_apollo_01`
  - `tranq20_01`
- profiles：
  - `baseline`
  - `experimental_l1_previous_dual_stabilization_20_only`
  - `experimental_l1_zero_dual_stabilization_20_only`
- repeat count = 2；
- time limit = 2.4s；
- max CG = 6。

结果：

- 5/10：
  - experimental profiles 仍为 no-op；
  - `dual_stabilization_events=0`；
  - official result 与同 repeat baseline 一致；
  - no critical disagreement。
- Apollo20 greedy-anchor：
  - previous-anchor 两次均 no-regression；
  - zero-anchor 一次从 `921.640296` 改善到 `848.242536`，一次 no-regression；
  - 改善未稳定复现。
- Tranq20：
  - previous-anchor 一次 no-regression，一次从 `781.398505` 改善到 `781.101309`；
  - zero-anchor 一次 improved，一次因 wall/pricing path 判为 worsened；
  - 效果不稳定。
- 所有 accepted stabilized dual event：
  - `current_pool_negative_count_max=0`；
  - `objective_mismatch_count=0`；
  - `current_pool_infeasible_count=0`。

Phase 9J 结论：

- dual stabilization 机制安全边界仍成立；
- 5/10 guard 仍成立；
- 20-task 有真实但不稳定的正向信号；
- zero-anchor 不能直接进入 production tuning；
- previous-anchor 较稳定但改善较弱；
- 当前仍不满足最终 A。

下一步建议：

- Phase 9K：扩大 repeat / 20-task hard set / time limit；
- 同时继续保留 5/10 no-regression；
- 如果改善仍不稳定或平均 wall 变差，应停止 dual-stabilization production 推进，转向 legacy final judge / profile-DP proof-tail optimization。

Phase 9K 已完成：

- 新增实例组：
  - `phase9k_dual_stabilization_gate`
- 新增 profile group：
  - `phase9k_rmp_dual_stabilization_hardset_ab`
- 修正 ROI 分类：
  - 20-task 非 OPTIMAL 状态下，如果 incumbent primal 变差，即使 wall time 更短也必须判为 `worsened`；
  - 防止“少算/更快 TIME_LIMIT 但 incumbent 更差”被误报为 improvement。

Smoke：

- 输出目录：
  - `BPC_future/results/sharded_pulse_phase9k_rmp_dual_stabilization_hardset_ab_smoke_20260613`
- 实例：
  - `apollo5`
  - `tranq5`
  - `apollo10`
  - `tranq10_09`
  - `tranq10_04`
  - `mt20_greedy_apollo_01`
  - `tranq20_01`
  - `mt20_greedy_tranq_01`
- profiles：
  - `baseline`
  - `experimental_l1_previous_dual_stabilization_20_only`
  - `experimental_l1_zero_dual_stabilization_20_only`
- repeat count = 3；
- time limit = 3.0s；
- max CG = 8；
- 共 72 行 summary。

结果：

- 5-task：
  - experimental profiles 仍为 no-op；
  - accepted stabilized dual = 0；
  - no official result change；
  - no critical disagreement。
- 10-task：
  - experimental profiles 仍为 no-op；
  - accepted stabilized dual = 0；
  - no official result change；
  - no critical disagreement。
- 20-task previous-anchor：
  - 9 行中 improved 6、worsened 2、no-regression 1；
  - accepted stabilized dual = 35；
  - `mt20_greedy_tranq_01` 三次 repeat 均把 primal 从 `761.814403` 改善到 `721.502279`；
  - `mt20_greedy_apollo_01` 和 `tranq20_01` 仍混合。
- 20-task zero-anchor：
  - 9 行中 improved 0、worsened 5、no-regression 4；
  - accepted stabilized dual = 42；
  - 不应继续作为候选 profile。
- 所有 accepted stabilized dual event：
  - `current_pool_negative_count_max=0`；
  - `objective_mismatch_count=0`；
  - `current_pool_infeasible_count=0`；
  - no critical disagreement。

Phase 9K 结论：

- previous-anchor 是当前唯一有继续验证价值的 dual stabilization 方向；
- zero-anchor 应暂停；
- 5/10 no-regression gate 仍成立；
- 20-task 有一个稳定正向 hard case，但 selected hard set 尚未整体稳定改善；
- 当前仍不满足最终 A。

下一步建议：

- Phase 9L：只保留 previous-anchor；
- 扩大 20-task selected hard set 和 repeat；
- 更完整地跑 5/10 no-regression gate；
- 若 previous-anchor 仍只在单个 hard case 改善或出现 20-task mixed/worsened，应停止 dual-stabilization production 推进，转向 legacy final judge / profile-DP proof-tail optimization。

Phase 9L 已完成：

- 新增实例组：
  - `phase9l_previous_dual_stabilization_gate`
- 新增 profile group：
  - `phase9l_previous_dual_stabilization_gate_ab`
- 只保留：
  - `baseline`
  - `experimental_l1_previous_dual_stabilization_20_only`
- 不再测试 zero-anchor。

Smoke：

- 输出目录：
  - `BPC_future/results/sharded_pulse_phase9l_previous_dual_stabilization_gate_ab_smoke_20260613`
- 实例：
  - `balanced5_all` 全量 20 个 5-task；
  - `balanced10_all` 全量 20 个 10-task；
  - `phase7o_20_smoke` 的 3 个可跑 20-task hard smoke；
- repeat count = 2；
- time limit = 3.0s；
- max CG = 8；
- 共 172 行 summary。

结果：

- 5-task full smoke gate：
  - baseline 40 行，previous-anchor 40 行；
  - previous-anchor `dual_stabilization_events=0`；
  - official result 无变化；
  - no critical disagreement；
  - avg wall `0.042747 -> 0.042789`。
- 10-task full smoke gate：
  - baseline 40 行，previous-anchor 40 行；
  - previous-anchor `dual_stabilization_events=0`；
  - official result 无变化；
  - pricing state 分布一致；
  - no critical disagreement；
  - avg wall `1.937340 -> 1.938006`。
- 20-task hard smoke：
  - previous-anchor 6 行中 improved 4、worsened 2；
  - accepted stabilized dual = 24；
  - `mt20_greedy_tranq_01` 两次 repeat 都稳定把 primal 从 `761.814403` 改善到 `721.502279`；
  - `tranq20_01` 一次 improved、一次 worsened；
  - `mt20_greedy_apollo_01` 一次 improved、一次 worsened。
- 所有 accepted stabilized dual event：
  - `current_pool_negative_count_max=0`；
  - `objective_mismatch_count=0`；
  - `current_pool_infeasible_count=0`；
  - no critical disagreement。

Phase 9L 结论：

- previous-anchor 对 5/10 是 no-op / no-regression；
- 20-task 仍有一个稳定正向 hard case；
- 但 selected 20-task hard smoke 仍然 mixed，并出现 worsened；
- previous-anchor 不能进入 production tuning；
- dual-stabilization production 扩张应暂停；
- 下一步应转向 legacy final judge / profile-DP proof-tail optimization。

Phase 10A 已完成：

- 新增全局 profile-DP proof-tail summary 字段：
  - `profile_dp_tail_records`
  - `profile_dp_tail_incomplete_count`
  - `profile_dp_tail_negative_count`
  - `profile_dp_tail_no_negative_count`
  - `profile_dp_tail_state_cap_hit_count`
  - `profile_dp_tail_mask_cap_incomplete_count`
  - `profile_dp_tail_time`
  - `profile_dp_tail_state_count_max`
  - `profile_dp_tail_processed_labels_max`
  - `profile_dp_tail_extension_attempts`
  - `profile_dp_tail_nonempty_mask_count_max`
  - `profile_dp_tail_max_labels_per_mask_observed_max`
  - `profile_dp_tail_top_mask_label_counts`
  - `profile_dp_tail_min_best_rc`
  - `profile_dp_tail_class`
  - `profile_dp_tail_reason`
- 新增 helper：
  - `_official_pricing_records()`
  - `_profile_dp_tail_metrics()`
  - `_classify_profile_dp_tail()`
- 新增 profile group：
  - `phase10a_profile_dp_tail_diagnostics`

Smoke：

- 输出目录：
  - `BPC_future/results/sharded_pulse_phase10a_profile_dp_tail_diagnostics_smoke_20260613`
- 实例：
  - `balanced5_all` 全量 20 个 5-task；
  - `balanced10_all` 全量 20 个 10-task；
  - `phase7o_20_smoke` 的 3 个可跑 20-task hard smoke；
- profile：
  - `baseline`
- repeat count = 1；
- time limit = 3.0s；
- max CG = 8；
- 共 43 行 summary。

结果：

- 5-task：
  - `profile_dp_tail_class` 主要是 `profile_dp_negative_tail`；
  - `profile_dp_tail_incomplete_count=0`；
  - `profile_dp_tail_state_cap_hit_count=0`；
  - 当前短 smoke 的 5-task TIME_LIMIT 不是 profile-DP incomplete/state-cap blocker。
- 10-task：
  - 20 行全部是 `profile_dp_negative_tail`；
  - `profile_dp_tail_state_cap_hit_count=56`；
  - 虽然多次触达 state cap，但多数仍返回 negative；
  - 当前更像 profile-DP search breadth / repeated negative tail，而非 no-negative proof certificate tail。
- 20-task：
  - `tranq20_01`：`profile_dp_incomplete_tail`
  - `mt20_greedy_apollo_01`：`profile_dp_incomplete_tail`
  - `mt20_greedy_tranq_01`：`profile_dp_negative_tail`
  - 20-task `profile_dp_tail_state_cap_hit_count=13`
  - 20-task `exact_completion_bound_retry_count=0`
  - 当前 hard tail 不是单纯 completion-bound retry 问题。

Phase 10A 结论：

- Pulse worker 和 dual-stabilization production 扩张都应继续暂停；
- 当前更明确的方向是 profile-DP proof-tail；
- 需要区分：
  - incomplete tail：`tranq20_01`、`mt20_greedy_apollo_01`
  - negative tail：`mt20_greedy_tranq_01`
- 下一步不应简单全局提高 cap，而应做 controlled A/B。

下一步建议：

- Phase 10B：profile-DP state-cap sensitivity / hotspot diagnostics；
- baseline cap 1000 vs modest cap 2000/3000；
- 只在 opt-in profile 下运行；
- 观察：
  - `profile_dp_tail_incomplete_count`
  - `profile_dp_tail_state_cap_hit_count`
  - wall time
  - pricing state
  - primal / negative return
  - top mask hotspots
- 若提高 cap 只增加 wall、不减少 incomplete，则转向 mask-hotspot ordering / selected-mask materialization；
- 不改变 certificate 语义。

============================================================
2026-06-13 Phase 10B profile-DP state-cap sensitivity
============================================================

本轮只做受控 cap sensitivity，不启用 Pulse worker、audit、dual stabilization 或 official certificate gate。

新增：

- `experimental_profile_dp_cap_2000_20_only`
- `experimental_profile_dp_cap_3000_20_only`
- `phase10b_profile_dp_state_cap_gate`
- `phase10b_profile_dp_state_cap_sensitivity`

cap profile 只在 20-task 生效；5/10 直接 no-op。

smoke：

- instances:
  - `apollo5`, `tranq5`
  - `apollo10`, `tranq10_09`, `tranq10_04`
  - `tranq20_01`, `mt20_greedy_apollo_01`, `mt20_greedy_tranq_01`
- profiles:
  - baseline
  - cap2000
  - cap3000
- output:
  - `BPC_future/results/sharded_pulse_phase10b_profile_dp_state_cap_sensitivity_smoke_20260613`
- report:
  - `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase10b_profile_dp_state_cap_sensitivity_zh.md`

结果：

- 5-task：
  - no-op guard 生效；
  - no official result change；
  - no critical disagreement。
- 10-task：
  - no-op guard 生效；
  - no official result change；
  - no critical disagreement。
- 20-task：
  - baseline: 1 incomplete, 2 found-negative；
  - cap2000: 2 incomplete, 1 found-negative；
  - cap3000: 1 incomplete, 2 found-negative；
  - cap2000/cap3000 都出现 20-task incumbent worsened；
  - cap3000 profile-DP time 明显上升。

关键观察：

- 简单提高 `journey_pricing_max_dp_states` 没有稳定降低 profile-DP incomplete tail；
- 更负 `best_rc` 不等于更好的 short CG tail；
- `mt20_greedy_apollo_01` 和 `mt20_greedy_tranq_01` 显示 cap 提高可能改变列进入顺序并让 incumbent 变差；
- 当前不应继续粗粒度加 cap。

下一步建议：

- Phase 10C：profile-DP mask-hotspot ordering / selected-mask materialization diagnostics；
- 重点解释 top-mask hotspot 和更负 RC 但 incumbent 变差的列顺序/列池问题；
- 继续保持：
  - 5/10 no-regression gate；
  - no Sharded Pulse default；
  - no official certificate gate；
  - no unsafe cut/subset-row prefix bound。

============================================================
2026-06-13 Phase 10C profile-DP mask-hotspot sensitivity
============================================================

本轮继续保持 calibration-only，不启用 Pulse worker、audit、dual stabilization 或 official certificate gate。

新增 summary 字段：

- `profile_dp_tail_label_cap_pruned`
- `profile_dp_tail_selected_candidate_input_count`
- `profile_dp_tail_selected_candidate_scanned_count`
- `profile_dp_tail_selected_candidate_materialized_count`
- `profile_dp_tail_selected_candidate_returned_count`
- `profile_dp_tail_selected_candidate_filtered_count`
- `profile_dp_tail_selected_unmaterialized_candidate_count`
- `profile_dp_tail_materialization_candidate_count`
- `profile_dp_tail_materialization_selected_candidate_count`
- `profile_dp_tail_materialization_infeasible_filtered_count`
- `profile_dp_tail_hotspot_class`
- `profile_dp_tail_hotspot_reason`

新增：

- `experimental_profile_dp_mask_label_cap_16_20_only`
- `experimental_profile_dp_mask_label_cap_32_20_only`
- `phase10c_profile_dp_mask_hotspot_gate`
- `phase10c_profile_dp_mask_hotspot_sensitivity`

profile 行为：

- 5/10 no-op；
- 20-task 设置 `journey_pricing_profile_dp_max_labels_per_mask=16/32`；
- 显式关闭：
  - Sharded Pulse audit；
  - Sharded Pulse hidden-negative worker；
  - dual stabilization。

smoke：

- output:
  - `BPC_future/results/sharded_pulse_phase10c_profile_dp_mask_hotspot_sensitivity_smoke_20260613`
- report:
  - `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase10c_profile_dp_mask_hotspot_sensitivity_zh.md`
- rows: 24

结果：

- 5-task：
  - no-op guard 生效；
  - no official result change；
  - no critical disagreement。
- 10-task：
  - no-op guard 生效；
  - no official result change；
  - no critical disagreement。
- 20-task：
  - baseline: 2 incomplete, 1 found-negative；
  - label-cap16: 2 incomplete, 1 found-negative；
  - label-cap32: 2 incomplete, 1 found-negative；
  - label-cap16 剪掉 574 个 labels，但没有减少 incomplete 或改善 incumbent；
  - label-cap32 在 `mt20_greedy_apollo_01` 单行改善 incumbent，但未触发 label-cap pruning，且 profile-DP time 上升；
  - selected candidates 全部 materialized 并 returned；
  - materialization candidate count 为 0。

关键观察：

- 当前 blocker 不是 selected-mask materialization failure；
- label-cap16 有真实 hotspot 剪枝信号，但没有 ROI；
- label-cap32 的单次 improvement 只能作为 repeat-validation 候选，不能作为主线证据；
- 不应把 label-cap 当成 proof-safe 或 production default。

下一步建议：

- Phase 10D：repeat validation；
- 对 `phase10c_profile_dp_mask_hotspot_sensitivity` 做 repeat-count 3；
- 判断 `label-cap32` 在 `mt20_greedy_apollo_01` 上是否稳定；
- 如果不稳定，转向 profile-DP ordering attribution / RMP column trajectory，而不是继续 label-cap。

============================================================
2026-06-13 Phase 10D profile-DP mask-hotspot repeat validation
============================================================

本轮只做 repeat validation，没有新增算法路径。

smoke：

- instances:
  - `apollo5`, `tranq5`
  - `apollo10`, `tranq10_09`, `tranq10_04`
  - `tranq20_01`, `mt20_greedy_apollo_01`, `mt20_greedy_tranq_01`
- profiles:
  - baseline
  - `experimental_profile_dp_mask_label_cap_16_20_only`
  - `experimental_profile_dp_mask_label_cap_32_20_only`
- repeat-count: 3
- rows: 72
- output:
  - `BPC_future/results/sharded_pulse_phase10d_profile_dp_mask_hotspot_repeat_smoke_20260613`
- report:
  - `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase10d_profile_dp_mask_hotspot_repeat_zh.md`

结果：

- 5-task：
  - no-op guard 生效；
  - 0 official changed；
  - 0 critical disagreement。
- 10-task：
  - no-op guard 生效；
  - 0 official changed；
  - 0 critical disagreement。
- 20-task：
  - baseline: 6 INCOMPLETE_LIMIT, 3 FOUND_NEGATIVE；
  - label-cap16: 6 INCOMPLETE_LIMIT, 3 FOUND_NEGATIVE；
  - label-cap32: 6 INCOMPLETE_LIMIT, 3 FOUND_NEGATIVE；
  - label-cap16 剪掉 1729 个 labels，但 incomplete 没下降；
  - label-cap16: 7 no_regression, 1 improved, 1 worsened；
  - label-cap32: 7 no_regression, 1 improved, 1 worsened；
  - no critical disagreement。

关键结论：

- label-cap16 有真实剪枝信号，但没有稳定 ROI；
- label-cap32 的 Phase 10C 单次 improvement 不稳定；
- 不应继续把 `profile_dp_max_labels_per_mask` 作为主线调参；
- 当前瓶颈更像 profile-DP ordering / column trajectory / RMP degeneracy，而不是 bucket width。

下一步建议：

- Phase 10E：profile-DP ordering attribution / RMP column trajectory；
- 对比 improved/worsened repeats 的 first returned task-set；
- 检查 returned task-set 与 active pool、later negative family、RMP objective/dual movement 的关系；
- 不启用 worker/default/certificate gate。

============================================================
2026-06-13 Phase 10E profile-DP ordering attribution
============================================================

本轮新增 summary diagnostics，不改变求解语义。

新增字段：

- `official_negative_journey_task_set_count`
- `official_negative_journey_task_set_hash`
- `official_negative_journey_task_set_samples`
- `official_negative_journey_sequence_samples`
- `official_negative_journey_signature_samples`
- `official_negative_first_task_set`
- `official_negative_first_task_count`
- `official_negative_profile_dp_top_overlap`
- `official_negative_profile_dp_top_jaccard`
- `official_negative_profile_dp_top_relation`
- `official_negative_profile_dp_top_exact`

smoke：

- instances:
  - `tranq20_01`
  - `mt20_greedy_apollo_01`
  - `mt20_greedy_tranq_01`
- profiles:
  - baseline
  - label-cap16
  - label-cap32
- repeat-count: 3
- rows: 27
- output:
  - `BPC_future/results/sharded_pulse_phase10e_profile_dp_ordering_attribution_smoke_20260613`
- report:
  - `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase10e_profile_dp_ordering_attribution_zh.md`

结果：

- returned negative task-set 与 profile-DP top-mask exact match 次数为 0；
- baseline returned negative task-set：
  - 2 overlapping；
  - 1 disjoint；
- label-cap16/32 returned negative task-set：
  - 全部 overlapping；
- `mt20_greedy_tranq_01` 最好的一行是 baseline repeat 2：
  - returned `[13,16]`；
  - 与 top-mask disjoint；
  - incumbent `721.502279`；
- label-cap16/32 在同一 repeat 返回 `[1,6,15]`：
  - 与 top-mask overlapping；
  - incumbent 回到 `761.814403`；
  - 说明 top-mask overlap 不等于好列。
- `mt20_greedy_apollo_01` 的变化没有 returned negative，可疑点是 active-pool / early trajectory divergence。

关键结论：

- 不要继续追 top-mask hotspot；
- 不要继续调 label cap；
- 当前更像 active-pool trajectory / RMP degeneracy / early column ordering 问题；
- Pulse worker、certificate gate 继续暂停。

下一步建议：

- Phase 10F：active-pool / early trajectory attribution；
- 记录前 N 个 pool active hash / top active task-set；
- 对比 baseline improved 与 label-cap worsened 的 early divergence；
- 判断是否是初始列、profile-DP returned order 或 RMP degeneracy 导致 short-time incumbent 分化。

2026-06-13 Phase 10F active-pool trajectory attribution
============================================================

Phase 10F 已完成 active-pool / early trajectory attribution：

- 新增 ROI summary 只读字段：
  - `pool_active_task_set_hash_first`
  - `pool_active_task_set_hash_sequence`
  - `pool_active_task_set_hash_unique_count`
  - `pool_active_task_set_hash_churn_count`
  - `pool_active_top_task_set_value_samples_first`
  - `pool_active_trajectory_class`
  - `pool_active_trajectory_reason`
- `_pool_structure_metrics()` 现在能汇总 active basis first/last/sequence、unique/churn count 与 first/last top active task-set samples。
- 新增 `_classify_active_pool_trajectory()`：
  - `no_pool_diagnostics`
  - `no_active_basis`
  - `stable_active_basis`
  - `churn_active_basis`
  - `high_churn_active_basis`
- 这些字段只读记录，不改变 pricing、RMP、worker trigger、certificate、lower-bound 或默认配置。

验证：

- focused tests：
  - `test_sharded_pulse_roi_calibration_phase7o_profiles_and_fields`
  - `test_sharded_pulse_roi_calibration_pool_structure_metrics_are_summarized`
  - `test_sharded_pulse_roi_calibration_profile_configs_are_opt_in`
  - 结果：`Ran 3 tests in 0.002s OK`
- smoke output：
  - `BPC_future/results/sharded_pulse_phase10f_active_pool_trajectory_attribution_smoke_20260613`
- report：
  - `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase10f_active_pool_trajectory_attribution_zh.md`

Smoke 矩阵：

- instances：
  - `tranq20_01`
  - `mt20_greedy_apollo_01`
  - `mt20_greedy_tranq_01`
- profiles：
  - baseline
  - label-cap16
  - label-cap32
- repeat-count：3
- rows：27

结果：

- 27 行全部无 critical disagreement；
- 三个 profiles 都保持：
  - 6 行 `INCOMPLETE_LIMIT`
  - 3 行 `FOUND_NEGATIVE`
- label-cap16/32 没有降低 20-task incomplete tail；
- label-cap16/32 各只改善 `mt20_greedy_apollo_01` 的 1 个 repeat；
- `tranq20_01` 三个 profiles 的 incumbent 都是 `781.101309`，active hash trajectory 稳定；
- `mt20_greedy_tranq_01` 三个 profiles 的 incumbent 都是 `761.814403`，active hash trajectory 完全一致；
- `mt20_greedy_apollo_01` 改善行从 baseline 常见 active trajectory 分叉到 `c36666e846435b59` 或 `98e14b42b7f3753c`。

关键结论：

- profile-DP top-mask / label-cap hotspot 不是稳定优化主线；
- label-cap 更像 early-column / active-pool trajectory perturbation，而不是 proof-tail fix；
- `mt20_greedy_apollo_01` 的短时 incumbent 改善更像 active-pool trajectory 分叉；
- 不应打开 Pulse worker default；
- 不应打开 official certificate gate；
- 不应继续扩大 label-cap tuning。

5/10 no-regression 口径：

- 本轮 smoke 只跑 20-task attribution；
- profiles 是 20-only calibration；
- 本轮不能作为新的 5/10 no-regression 证据；
- 因为仅新增 summary 字段和测试，默认 benchmark 行为不变；
- 仍需后续 phase 按目标要求补 5/10 gate。

下一步建议：

- Phase 10G：early-column / active-pool trajectory controlled attribution；
- 继续只做 calibration / read-only 或极窄 experimental profile；
- 比较 early added-column order 与 active hash transition；
- 判断改善 active families 是否可由初始列池或 early profile-DP returned order 复现；
- 不要继续 label-cap；
- 不要放开 worker / certificate / 20-task production default。

2026-06-13 Phase 10G early-column / active-pool attribution
============================================================

Phase 10G 已完成 early-column / active-pool attribution：

- 新增 ROI summary 只读字段：
  - `early_column_addition_events`
  - `early_column_addition_kind_sequence`
  - `early_column_primary_task_set_sequence`
  - `early_column_changed_task_set_hash_sequence`
  - `early_column_new_task_set_hash_sequence`
  - `early_column_productivity_class_sequence`
  - `early_column_active_hash_before_sequence`
  - `early_column_active_hash_after_sequence`
  - `early_column_active_hash_transition_count`
  - `early_column_changed_active_relation_before_sequence`
  - `early_column_changed_active_relation_after_sequence`
  - `early_column_active_changed_task_set_count`
  - `early_column_trajectory_class`
  - `early_column_trajectory_reason`
- 新增 `_early_column_trajectory_metrics()`，只从已有 JSONL 的 `journey_column_addition` 与 `journey_pool_structure_diagnostics` 还原 early trajectory；
- 新增 `_classify_early_column_trajectory()`；
- 新增 focused test：
  - `test_sharded_pulse_roi_calibration_early_column_trajectory_metrics_are_summarized`

验证：

- focused tests：
  - `test_sharded_pulse_roi_calibration_phase7o_profiles_and_fields`
  - `test_sharded_pulse_roi_calibration_pool_structure_metrics_are_summarized`
  - `test_sharded_pulse_roi_calibration_early_column_trajectory_metrics_are_summarized`
  - `test_sharded_pulse_roi_calibration_profile_configs_are_opt_in`
  - 结果：`Ran 4 tests in 0.002s OK`
- smoke output：
  - `BPC_future/results/sharded_pulse_phase10g_early_column_active_pool_attribution_smoke_20260613`
- report：
  - `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase10g_early_column_active_pool_attribution_zh.md`

Smoke 矩阵：

- instances：
  - `tranq20_01`
  - `mt20_greedy_apollo_01`
  - `mt20_greedy_tranq_01`
- profiles：
  - baseline
  - label-cap16
  - label-cap32
- repeat-count：3
- rows：27

结果：

- 27 行全部无 critical disagreement；
- 三个 profiles 都保持：
  - 6 行 `INCOMPLETE_LIMIT`
  - 3 行 `FOUND_NEGATIVE`
- 全部 27 行 `early_column_trajectory_class=inactive_addition_enters_active_basis`；
- 含义：
  - early additions 当下 `active_changed_task_set_count=0`；
  - 加列主要以 inactive task-set 进入 pool；
  - 但后续 active-basis samples 中出现这些 task-set；
  - 当前分叉更像 inactive column 后续进入 active basis，而不是 active-support-changing column 立即生效。

实例结果：

- `tranq20_01`：
  - 三个 profiles 三次 repeat 都是 `781.101309`；
  - early task-set sequence 稳定：
    - `[5,15,20]`
    - `[4,13,18]`
    - `[4,6,13]`
- `mt20_greedy_tranq_01`：
  - 三个 profiles 三次 repeat 都是 `761.814403`；
  - early task-set sequence 完全一致；
  - label-cap 无 ROI。
- `mt20_greedy_apollo_01`：
  - baseline 本轮已有 1 个 repeat 到 `847.812231`；
  - label-cap32 有 2 行 changed vs same-repeat baseline：
    - `921.640296 -> 847.812231`
    - `921.640296 -> 773.85915`
  - 改善行伴随更长 early inactive-to-active 链与 active hash 分叉：
    - `c36666e846435b59`
    - `98e14b42b7f3753c`
    - `c9dd1125cc1bf8fa`

关键结论：

- label-cap 不是稳定 proof-tail optimization；
- Apollo 改善更像 early inactive column 后续进入 active basis 的 trajectory effect；
- 这仍然只是 short-time trajectory effect，不是稳定求解加速；
- 不应打开 worker default；
- 不应打开 official certificate gate；
- 不应继续扩大 label-cap。

5/10 no-regression 口径：

- 本轮 smoke 仍只跑 20-task attribution；
- profiles 是 20-only calibration；
- 本轮不能作为新的 5/10 no-regression 证据；
- 因为仅新增 summary 字段和测试，默认 benchmark 行为不变。

下一步建议：

- Phase 10H：early-column controlled intervention / negative-result split；
- 如果继续 intervention，只能是 20-only calibration profile；
- 不允许 certificate effect；
- 必须补 5/10 no-op gate；
- 如果 intervention 仍不稳定，停止 profile-DP / worker / label-cap 主线，输出 negative-result synthesis。

============================================================
2026-06-13 Phase 10H 结论：early-column quota 是真实干预，但 ROI 不稳定
============================================================

本轮实现：

- 新增 20-only calibration profiles：
  - `experimental_early_new_task_set_quota_3_20_only`
  - `experimental_early_new_task_set_quota_3_return12_20_only`
- 新增 profile group：
  - `phase10h_early_new_task_set_quota`
- profile 只在 `task_count >= 20` 生效；
- 显式关闭：
  - Sharded Pulse audit
  - hidden-negative worker
  - dual stabilization
  - certificate / official lower-bound effect

验证：

- focused tests：
  - `test_sharded_pulse_roi_calibration_phase7o_profiles_and_fields`
  - `test_sharded_pulse_roi_calibration_profile_configs_are_opt_in`
  - 结果：`Ran 2 tests in 0.002s OK`
- 语法检查：
  - `run_sharded_pulse_roi_calibration.py`
  - `test_bpc_future.py`
  - 结果：通过
- full regression：
  - `BPC_future.tests.test_bpc_future.BPCFutureTests`
  - 结果：`Ran 483 tests in 1.453s OK (skipped=1)`
- `git diff --check`：
  - 结果：通过
- 20-task smoke output：
  - `BPC_future/results/sharded_pulse_phase10h_early_new_task_set_quota_smoke_20260613`
- 5/10 guard output：
  - `BPC_future/results/sharded_pulse_phase10h_early_new_task_set_quota_5_10_guard_20260613`
- report：
  - `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase10h_early_new_task_set_quota_zh.md`

5/10 guard：

- instances：
  - `apollo5`
  - `tranq5`
  - `apollo10`
  - `tranq10_09`
  - `tranq10_04`
- profiles：
  - baseline
  - return8 quota
  - return12 quota
- 结果：
  - 两个 experimental profiles 均为 no-op；
  - 10 行 experimental rows 全部 `official_result_changed_vs_baseline=False`；
  - primal/status/pricing_state 与 baseline 完全一致；
  - 5/10 无回退。

20-task smoke：

- instances：
  - `tranq20_01`
  - `mt20_greedy_apollo_01`
  - `mt20_greedy_tranq_01`
- repeat-count：3
- rows：27
- 结果：
  - 27 行全部无 critical disagreement；
  - 三个 profiles 都保持：
    - 6 行 `INCOMPLETE_LIMIT`
    - 3 行 `FOUND_NEGATIVE`
  - incomplete tail 未下降。

20-task incumbent 分裂：

- `tranq20_01`：
  - baseline 三次：`781.101309`
  - return8：`597.118613`, `596.176491`, `594.045835`
  - return12：`605.126958`, `593.924951`, `605.126958`
  - 稳定改善
- `mt20_greedy_apollo_01`：
  - baseline：`847.812231`, `921.640296`, `921.640296`
  - return8：`1061.554044`, `1061.554044`, `770.211317`
  - return12：三次都是 `1061.554044`
  - 大多回退，仅 return8 一次改善
- `mt20_greedy_tranq_01`：
  - baseline 三次：`761.814403`
  - return8 三次：`829.395319`
  - return12 三次：`704.228463`
  - 对 quota 参数方向敏感

关键判断：

- early new-task-set quota 能真实改变 early-column / active-pool 轨迹；
- 但它不是稳定优化；
- 它没有降低 proof tail / incomplete tail；
- 它不能默认启用；
- 它不能进入 official certificate gate；
- 它不能作为继续扩大 Pulse worker 的理由。

当前路线判断：

- profile-DP label cap 不稳定；
- early-column quota 不稳定；
- worker/probe 多轮 A/B 没有稳定 ROI；
- 当前证据已经更接近最终交付条件 B：
  - 当前 Pulse worker/proof 路线在安全约束下 ROI 不成立；
  - 下一步应整理 negative-result synthesis；
  - 或转向 RMP stabilization / pool compression / legacy final judge optimization。

============================================================
2026-06-13 Phase 10I：最终条件 B 完成性审计
============================================================

报告：

- `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase10i_negative_result_completion_audit_zh.md`

审计目标：

- 对最终条件 B 做逐项检查；
- 区分已经可以关闭的 Pulse 子路线和仍未完成的 proof-route 缺口；
- 避免把 active-worker 负结果误当作整个目标完成。

已可关闭的子路线：

1. Active hidden-negative worker / current-context probe：
   - 连续多轮 A/B 无稳定 wall-time ROI；
   - worker 可安全加 true-RC negative columns；
   - 但 worker 加列不稳定降低 residual pricing tail；
   - 5/10 no-regression 主要靠 gate 关闭 worker；
   - 不应继续加 worker budget / gate / target-specific ordering。

2. Profile-DP cap / mask label-cap：
   - state-cap 提高无稳定 ROI；
   - label-cap 改变 Apollo short-time trajectory，但不降低 incomplete tail；
   - 不应继续扩大 label-cap 或简单提高 DP cap。

3. Early-column new-task-set quota：
   - 5/10 guard 通过；
   - 20-task 确实改变 active-pool 轨迹；
   - 但 `mt20_greedy_apollo_01` 大多回退，`mt20_greedy_tranq_01` 对 quota 参数方向敏感；
   - incomplete tail 不下降；
   - 不能默认启用，也不能进入 certificate / worker gate。

最终条件 B 当前状态：

| 子项 | 状态 |
|---|---|
| 连续两轮 A/B 无改善 | active-worker / profile-DP / early-quota 子路线已满足 |
| worker 加列不降低 tail | active-worker 子路线已满足 |
| 20-task 没明显稳定改善 | 当前候选 profiles 已满足 |
| no critical disagreement | 已满足 |
| refinement 无降低 incomplete | 证据不完整：7J 只证明 exactness，未证明 production incomplete 降低 |
| resume 无降低 incomplete | 未满足：proof-closed Sharded Pulse resume 未实现/验证 |

关键结论：

- 当前不能标记目标完成；
- 条件 A 未满足；
- 条件 B 仍缺 `refinement/resume 无降低 incomplete` 的 full proof-route 证据；
- 条件 C 未触发；
- 下一步不能继续扩大 Pulse worker / label-cap / early quota；
- 下一步要么补 proof-route negative evidence，要么转向 RMP stabilization / pool compression / legacy final judge proof-tail optimization。

验证：

- `git diff --check`：通过；
- full regression：
  - `BPC_future.tests.test_bpc_future.BPCFutureTests`
  - 结果：`Ran 483 tests in 1.439s OK (skipped=1)`。

============================================================
2026-06-13 Phase 11A：profile-DP pricing-time sensitivity
============================================================

报告：

- `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase11a_profile_pricing_time_sensitivity_zh.md`

本轮实现：

- 新增 20-only calibration profiles：
  - `experimental_pricing_time_0_6_20_only`
  - `experimental_pricing_time_1_0_20_only`
- 新增 profile group：
  - `phase11a_profile_pricing_time_sensitivity`
- profile 只在 `task_count >= 20` 生效；
- 分别设置：
  - `journey_pricing_time_limit=0.6`
  - `journey_pricing_time_limit=1.0`
- 显式关闭：
  - Sharded Pulse audit
  - hidden-negative worker
  - dual stabilization
  - certificate / official lower-bound effect

验证：

- focused tests：
  - `test_sharded_pulse_roi_calibration_phase7o_profiles_and_fields`
  - `test_sharded_pulse_roi_calibration_profile_configs_are_opt_in`
  - 结果：`Ran 2 tests in 0.002s OK`
- 语法检查：
  - `run_sharded_pulse_roi_calibration.py`
  - `test_bpc_future.py`
  - 结果：通过
- full regression：
  - `BPC_future.tests.test_bpc_future.BPCFutureTests`
  - 结果：`Ran 483 tests in 1.448s OK (skipped=1)`
- `git diff --check`：
  - 结果：通过
- 20-task smoke output：
  - `BPC_future/results/sharded_pulse_phase11a_profile_pricing_time_sensitivity_smoke_20260613`
- 5/10 guard output：
  - `BPC_future/results/sharded_pulse_phase11a_profile_pricing_time_sensitivity_5_10_guard_20260613`

5/10 guard：

- instances：
  - `apollo5`
  - `tranq5`
  - `apollo10`
  - `tranq10_09`
  - `tranq10_04`
- 结果：
  - 两个 experimental profiles 均为 no-op；
  - `official_result_changed_vs_baseline=False`；
  - primal/status/pricing_state 与 baseline 完全一致；
  - 5/10 无回退。

20-task smoke：

- instances：
  - `tranq20_01`
  - `mt20_greedy_apollo_01`
  - `mt20_greedy_tranq_01`
- repeat-count：2
- rows：18
- 结果：
  - baseline：4 行 `INCOMPLETE_LIMIT`，2 行 `FOUND_NEGATIVE`
  - 0.6s：6 行 `INCOMPLETE_LIMIT`
  - 1.0s：6 行 `INCOMPLETE_LIMIT`
  - critical disagreement：0

20-task 细节：

- `tranq20_01`：
  - 0.6s profile 两次改善 incumbent 到 `676.808421`，但仍 `INCOMPLETE_LIMIT` 且 wall 接近 3s cap；
  - 1.0s profile 两次回退到 `781.398505`。
- `mt20_greedy_apollo_01`：
  - 0.6s profile 两次 `837.187019`，一行改善、一行相对 baseline 847 回退；
  - 1.0s profile 两次 `849.288754`，一行改善、一行回退。
- `mt20_greedy_tranq_01`：
  - baseline 两次 `FOUND_NEGATIVE`，primal `761.814403`；
  - 0.6s profile 两次变成 `INCOMPLETE_LIMIT`，primal 不变；
  - 1.0s profile 两次 `INCOMPLETE_LIMIT` 且 primal 回退到 `829.395319`。

关键判断：

- 简单增加 `journey_pricing_time_limit` 没有降低 incomplete；
- 反而把 baseline 中的 `FOUND_NEGATIVE` rows 变成 `INCOMPLETE_LIMIT`；
- 0.6s 的部分 incumbent 改善不是 proof-tail 改善，且 wall 触顶；
- 1.0s 大多回退；
- 这条结果进一步排除“粗粒度增加 pricing time”作为主线优化。

当前路线判断：

- 不继续扩大 Pulse worker；
- 不继续扩大 profile-DP cap / label-cap；
- 不继续调 early-column quota；
- 不简单增加 pricing time；
- 若继续追求条件 A，应关注更结构化的 candidate ordering / returned-column selection / RMP trajectory；
- 若继续完成条件 B，仍需处理 proof-closed resume / refinement-resume 缺口。

============================================================
2026-06-13 Phase 11B：profile-DP selection-mode sensitivity
============================================================

报告：

- `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase11b_profile_selection_mode_sensitivity_zh.md`

本轮实现：

- 新增 20-only calibration profiles：
  - `experimental_profile_selection_integer_diverse_20_only`
  - `experimental_profile_selection_orthogonal_20_only`
- 新增 profile group：
  - `phase11b_profile_selection_mode_sensitivity`
- profile 只在 `task_count >= 20` 生效；
- 分别设置：
  - `journey_pricing_selection_mode`
  - `journey_heuristic_selection_mode`
- 显式关闭：
  - Sharded Pulse audit
  - hidden-negative worker
  - dual stabilization
  - certificate / official lower-bound effect

验证：

- focused tests：
  - `test_sharded_pulse_roi_calibration_phase7o_profiles_and_fields`
  - `test_sharded_pulse_roi_calibration_profile_configs_are_opt_in`
  - 结果：`Ran 2 tests in 0.002s OK`
- 语法检查：
  - `run_sharded_pulse_roi_calibration.py`
  - `test_bpc_future.py`
  - 结果：通过
- full regression：
  - `BPC_future.tests.test_bpc_future.BPCFutureTests`
  - 结果：`Ran 483 tests in 1.422s OK (skipped=1)`
- `git diff --check`：
  - 结果：通过
- 20-task state-cap smoke output：
  - `BPC_future/results/sharded_pulse_phase11b_profile_selection_mode_sensitivity_smoke_20260613`
- 20-task activation smoke output：
  - `BPC_future/results/sharded_pulse_phase11b_profile_selection_mode_sensitivity_state1000_smoke_20260613`
- 5/10 guard output：
  - `BPC_future/results/sharded_pulse_phase11b_profile_selection_mode_sensitivity_5_10_guard_20260613`

5/10 guard：

- instances：
  - `apollo5`
  - `tranq5`
  - `apollo10`
  - `tranq10_09`
  - `tranq10_04`
- rows：15
- 结果：
  - 两个 experimental profiles 均为 no-op；
  - `official_result_changed_vs_baseline=False`；
  - critical disagreement = 0；
  - primal/status/pricing_state 与 baseline 完全一致；
  - 5/10 无回退。

20-task state-cap smoke：

- instances：
  - `tranq20_01`
  - `mt20_greedy_apollo_01`
  - `mt20_greedy_tranq_01`
- repeat-count：2
- with `pricing_max_dp_states=1`：
  - all profiles had 6 `INCOMPLETE_LIMIT` rows；
  - `profile_dp_tail_selected_candidate_input/materialized/returned = 0/0/0`；
  - selected-column mode 没有机会介入；
  - critical disagreement = 0。

20-task activation smoke：

- same instances；
- repeat-count：2
- with `pricing_max_dp_states=1000`：
  - all profiles had 6 `FOUND_NEGATIVE` rows；
  - integer-diverse / orthogonal 没有改变 official outcome；
  - selected-candidate counts 与 baseline 一致：72 input / 18 materialized / 18 returned；
  - orthogonal 在一个 `mt20_greedy_tranq_01` repeat 改变 returned task set，但没有改善 primal/status；
  - critical disagreement = 0。

关键判断：

- returned-column selection mode 可以改变个别返回列，但没有稳定 ROI；
- 在最硬 state-cap 设置下，blocker 发生在 selection 之前；
- 在 selection 真正运行时，也没有减少 tail、没有改善 official result；
- 这条子路线不应继续作为主线调参方向。

当前路线判断：

- 不继续扩大 Pulse worker；
- 不继续扩大 profile-DP cap / label-cap / pricing time；
- 不继续调 early-column quota 或 selection mode；
- 若完成最终条件 B，应补 proof-closed resume / refinement-resume 的负证据，再输出完整 negative-result / pivot report；
- 若继续性能路线，应转向 RMP stabilization / pool compression / legacy final judge proof-tail optimization。

============================================================
2026-06-13 Phase 11C：proof-route refinement / resume audit
============================================================

报告：

- `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase11c_proof_route_refinement_resume_audit_zh.md`

目标：

- 审计最终条件 B 仍缺的 refinement / proof-closed resume 证据；
- 不把 worker / selection / budget 的负结果误包装成完整 proof-route 负结果。

静态实现审计：

- `sharded_pulse_final_judge.py` 当前是 ledger / cache-key / dummy scaffolding；
- `ShardProofRecord.proof_closed=False` 不能证书化；
- frontier snapshot 已被测试为非 proof；
- `JourneyPricingConfig.pulse_resume_enabled` 字段存在，但当前没有 Sharded Pulse proof-closed persistent resume 消费路径；
- 现有可运行 resume 主要是 profile catalog / profile-label resume，不等价于 shard proof-ledger resume。

Dynamic smoke：

- output：
  - `BPC_future/results/sharded_pulse_phase11c_proof_route_refinement_resume_audit_20260613`
- instances：
  - `tranq20_01`
  - `mt20_greedy_apollo_01`
  - `mt20_greedy_tranq_01`
- profiles：
  - baseline
  - `audit_no_refine`
  - `audit_refine`
- repeat-count：2
- rows：18

验证：

- py_compile：
  - `sharded_pulse_final_judge.py`
  - `pulse_toy_exhaustive.py`
  - `run_sharded_pulse_roi_calibration.py`
  - `test_bpc_future.py`
  - 结果：通过
- focused tests：
  - refined parent / frontier-not-proof / cache-key / adaptive refinement / low-ROI gate
  - 结果：`Ran 8 tests in 0.008s OK`
- full regression：
  - `BPC_future.tests.test_bpc_future.BPCFutureTests`
  - 结果：`Ran 483 tests in 1.455s OK (skipped=1)`
- `git diff --check`：
  - 结果：通过

Official result guard：

- `critical_disagreement_count=0`
- `official_result_changed_vs_baseline=False`
- all official status = `TIME_LIMIT`
- all official pricing_state = `INCOMPLETE_LIMIT`
- worker events = 0
- no certificate / lower-bound effect

20-task audit 聚合：

- `audit_no_refine`：
  - audit status：6 `FOUND_NEGATIVE`
  - comparison：6 `legacy_incomplete_pulse_negative`
  - shards total/certified/incomplete/negative/refined：
    - 120 / 2 / 108 / 10 / 0
  - harvested：46
  - recursions：6070
- `audit_refine`：
  - audit status：6 `FOUND_NEGATIVE`
  - comparison：6 `legacy_incomplete_pulse_negative`
  - shards total/certified/incomplete/negative/refined：
    - 120 / 2 / 108 / 10 / 0
  - harvested：46
  - recursions：6090

关键判断：

- `audit_refine` 没有降低 incomplete；
- `audit_refine` 没有增加 certified shards；
- `final_judge_shards_refined=0`，说明当前 hard-tail audit 形态很快进入 negative-shard signal，而不是 no-negative proof-completion/refinement path；
- second-action refinement 在本轮 20-task hard-tail smoke 中没有形成 ROI；
- proof-closed resume 仍未实现 / 未验证。

当前路线判断：

- 条件 A：未满足，20-task 没有稳定改善和求解加速；
- 条件 B：仍未满足，虽然 refinement 有本轮负证据，但 proof-closed resume 子项仍未关闭；
- 条件 C：未发现 correctness blocker。

下一步：

- 不继续扩大 worker / pricing time / state cap / label cap / early quota / selection mode / refinement threshold；
- 若完成条件 B，需要：
  - 实现最小 proof-closed resume skeleton 并做 exact-safe smoke；
  - 或明确将 proof-closed resume 从当前路线移除，写完整 negative-result / pivot report。

============================================================
2026-06-13 Phase 11D：final negative-result / pivot
============================================================

报告：

- `BPC_future/logical_graph/run_reports/20260613_sharded_pulse_phase11d_final_negative_result_pivot_zh.md`

目标：

- 不继续写 Pulse 算法；
- 不实现新的 proof-closed resume；
- 基于 Phase 7O 到 Phase 11C 的证据，正式关闭当前 `Sharded Pulse worker/proof` 路线的继续扩张；
- 明确将 proof-closed resume 从当前路线移除，作为未来新 proof-route 能力，而不是当前路线的已实现组件。

关键边界：

- `ShardProofRecord.proof_closed=False` 不能 certificate；
- frontier snapshot 已被测试为非 proof；
- `JourneyPricingConfig.pulse_resume_enabled` 字段存在，但没有 Sharded Pulse proof-closed persistent resume 消费路径；
- 现有 profile catalog / profile-label resume 不等价于 shard proof-ledger resume；
- 本轮不把未实现 resume 伪装成已验证机制。

证据汇总：

- Phase 7O hard-tail worker ROI A/B：
  - rows：24
  - official status：24 / 24 `TIME_LIMIT`
  - official pricing state：24 / 24 `INCOMPLETE_LIMIT`
  - critical disagreement：0
  - worker events：14
  - legacy final judge calls：48
  - completion-bound retry count：0
  - 结论：worker 能安全进入 add-column path，但没有稳定降低 hard tail。
- Phase 8Q passed-source ROI validation：
  - rows：35
  - official status：35 / 35 `TIME_LIMIT`
  - critical disagreement：0
  - worker returned / added journeys：10 / 10
  - worker added new task sets：8
  - worker added support-changing：2
  - 结论：能加 true-RC negative columns，但不等于能减少 tail。
- Phase 9J dual stabilization repeat A/B：
  - rows：36
  - official status：36 / 36 `TIME_LIMIT`
  - critical disagreement：0
  - 结论：轻量 RMP/dual stabilization diagnostic 没有形成目标 A 所需改善。
- Phase 11B profile selection 5/10 guard：
  - rows：15
  - official result changed：0
  - critical disagreement：0
  - 结论：20-only selection 调参没有破坏 5/10，但也没有带来目标收益。
- Phase 11C refinement audit：
  - `audit_no_refine` shards total/certified/incomplete/negative/refined：
    - 120 / 2 / 108 / 10 / 0
  - `audit_refine` shards total/certified/incomplete/negative/refined：
    - 120 / 2 / 108 / 10 / 0
  - 结论：adaptive refinement 没有降低 incomplete，没有增加 certified shards。

Final condition audit：

- 条件 A：未满足；20-task selected hard set 没有稳定 wall-time / status / gap 改善。
- 条件 B：对当前已实现的 Pulse worker/proof 路线，满足负结果交付：
  - 连续多轮 A/B 无稳定改善；
  - worker 加 true-RC negative columns 不降低 tail；
  - adaptive refinement 无降低 incomplete；
  - proof-closed resume 未实现，已从当前路线移除，不再作为继续扩大当前路线的理由；
  - 20-task 没有明显改善；
  - no critical disagreement；
  - no certificate / lower-bound side effect。
- 条件 C：未触发；没有 correctness blocker。

最终路线判断：

- 停止继续扩大当前 Pulse worker/proof 路线；
- 不默认启用 worker；
- 不开放 official certificate gate；
- 不继续扩大 worker budget / pricing time / profile-DP cap / label cap / early quota / selection mode / refinement threshold；
- proof-closed resume 若未来重启，应作为新 Phase，从 proof-closed record 持久化、context hash、fresh/resume exactness 对照开始；
- 后续主线 pivot 到：
  - RMP stabilization / active-family stabilization；
  - column pool compression / column impact filter；
  - legacy final judge / profile-DP proof-tail optimization；
  - returned-column quality 与 RMP trajectory 的直接因果诊断。

验证：

- py_compile：
  - `sharded_pulse_final_judge.py`
  - `pulse_toy_exhaustive.py`
  - `run_sharded_pulse_roi_calibration.py`
  - `test_bpc_future.py`
  - 结果：通过
- focused tests：
  - refined parent / frontier-not-proof / cache-key / adaptive refinement / low-ROI gate
  - 结果：`Ran 8 tests in 0.005s OK`
- full regression：
  - `BPC_future.tests.test_bpc_future.BPCFutureTests`
  - 结果：`Ran 483 tests in 1.431s OK (skipped=1)`
- `git diff --check`：
  - 结果：通过

============================================================
必须持续遵守的验证流程
============================================================

每个 phase 完成后都要运行：
1. py_compile changed files
2. git diff --check
3. focused tests for that phase
4. all sharded/pulse/certificate focused regression
5. 若改 driver official result，跑 BPCFutureTests 全量
6. 若做 A/B，保存 summary.json / summary.csv / markdown report

至少保留：
- BPC_future/docs/sharded_pulse_final_judge_plan.md 更新
- BPC_future/logical_graph/run_reports/<phase>_zh.md
- BPC_future/results/<phase>/summary.json
- BPC_future/results/<phase>/summary.csv

所有报告必须写：
- 做了什么；
- 没做什么；
- exactness 边界；
- tests run；
- smoke matrix；
- official result 是否改变；
- critical disagreement；
- 5-task no-regression；
- 10-task no-regression；
- 20-task improvement；
- ROI 判断；
- 下一步建议。

============================================================
硬性禁止
============================================================

不要：
- 默认启用 Sharded Pulse；
- 牺牲 5-task 或 10-task 去换 20-task；
- 让 Pulse no-column 证书化，除非处于极窄 experimental certificate gate；
- 让 worker incomplete / duplicate-only / empty-harvest 影响 official lower bound；
- 使用 smoothed/GNN dual 做 certificate；
- 使用 unsafe epsilon dominance；
- 使用 unsafe first-task/next-task lexicographic symmetry breaking；
- 每个 label 解 LP/MIP；
- 重新启用 RPCE/full AMCB/per-label TWR-HK 作为主线；
- 为追求剪枝把 cuts/subset-row contribution 未证明地加入 prefix bound；
- 无上下文 hash 复用 audit/worker/resume 信号；
- 因为一个 20-task smoke 好看就放开 10/20 official certificate；
- 忽略 small-fast Apollo5 / Tranq5 的 overhead regression；
- 只报告 20-task，必须同时报告 5/10 no-regression。

============================================================
最终交付要求
============================================================

请持续迭代，直到满足以下任一条件：

A. 达到目标：
- 5-task 无回退；
- 10-task 无回退；
- 20-task selected hard set 有明显改善和求解加速；
并且 exactness tests / no critical disagreement 全部通过。

B. 证明当前 Pulse worker/proof 路线在安全约束下无 ROI：
- 连续两轮 A/B 无改善；
- worker 加列不降低 tail；
- refinement/resume 无降低 incomplete；
- 20-task 没明显改善；
则停止继续扩大 Pulse，输出 negative-result report 和建议转向 RMP stabilization / pool compression / legacy final judge optimization。

当前状态：

- 2026-06-13 Phase 11D 已按当前已实现路线完成 B 类 negative-result / pivot 交付；
- `resume` 子项不是通过实验证明完成，而是因为 Sharded Pulse proof-closed resume 当前未实现，被明确移出当前路线；
- 若未来要重新评估 resume，必须作为新的 proof-route Phase，不影响本轮对当前路线的停止结论。

C. 发现 correctness blocker：
- critical disagreement；
- true-RC mismatch；
- forbidden/branch/cut context mismatch；
立即停止性能优化，修 exactness bug。

不要在未达到 5/10 no-regression 与 20 improvement 证据前宣称“性能已提升”。
